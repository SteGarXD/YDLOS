#!/usr/bin/env bash
# Remove Cursor co-author trailer from commit messages (YDL OS policy: author only SteGar).
MSG_FILE="$1"
if [[ -f "$MSG_FILE" ]]; then
  sed -i '/^Co-authored-by: Cursor/d' "$MSG_FILE"
fi
