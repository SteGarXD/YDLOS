diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisX/AxisX.js b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisX/AxisX.js
index 3444e9d..7911a03 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisX/AxisX.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisX/AxisX.js
@@ -1,6 +1,6 @@
 import React from 'react';
 import { line, select } from 'd3';
-import { block, getAxisTitleRows, getBandsPosition, getLabelFormatter, getLineDashArray, getMaxTickCount, getTicksCount, handleOverflowingText, } from '../../utils';
+import { block, getAxisTitleRows, getBandsPosition, getLabelFormatter, getLineDashArray, getMaxTickCount, getTicksCount, handleOverflowingText, isBandScale, } from '../../utils';
 import { axisBottom } from '../../utils/chart/axis-generators';
 import './styles.css';
 const b = block('axis');
@@ -28,15 +28,20 @@ export function getTitlePosition(args) {
     return { x, y };
 }
 export const AxisX = React.memo(function AxisX(props) {
-    const { axis, boundsOffsetLeft, boundsOffsetTop, height: totalHeight, htmlLayout, leftmostLimit, plotAfterRef, plotBeforeRef, split, scale, width, } = props;
+    const { axis, boundsOffsetLeft, boundsOffsetTop, height: totalHeight, htmlLayout, leftmostLimit, plotAfterRef, plotBeforeRef, split, scale, width, xAxisBaselineY, } = props;
+    const plotBottomY = typeof xAxisBaselineY === 'number' && Number.isFinite(xAxisBaselineY) ? xAxisBaselineY : totalHeight;
     const ref = React.useRef(null);
     React.useEffect(() => {
+        let cancelled = false;
         (async () => {
             if (!ref.current || !htmlLayout) {
                 return;
             }
             const svgElement = select(ref.current);
             svgElement.selectAll('*').remove();
+            if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
+                select(plotAfterRef.current).selectAll('[data-gcharts-x-corner-tick]').remove();
+            }
             if (!axis.visible) {
                 return;
             }
@@ -54,7 +59,7 @@ export const AxisX = React.memo(function AxisX(props) {
                 boundsOffsetLeft,
                 boundsOffsetTop,
                 domain: {
-                    size: width,
+                    size: width + 7,
                     color: axis.lineColor,
                 },
                 htmlLayout,
@@ -73,12 +78,125 @@ export const AxisX = React.memo(function AxisX(props) {
                     labelsStyle: axis.labels.style,
                     maxTickCount: getMaxTickCount({ axis, width }),
                     rotation: axis.labels.rotation,
+                    zeroLabelVerticalNudgePx: axis.zeroLabelVerticalNudgePx,
+                    zeroLabelHNudgePx: axis.zeroLabelHNudgePx,
                 },
             });
+            if (cancelled || !ref.current) {
+                return;
+            }
             svgElement.call(xAxisGenerator).attr('class', b());
+            // Use <rect fill> instead of <path stroke> for all axis lines.
+            // A stroked path at integer coordinates straddles pixel boundaries and renders as
+            // 2 physical pixels at DPR≠1 (e.g. Windows 125% scaling = DPR 1.25).
+            // A filled <rect> at physical-pixel-aligned coordinates is guaranteed 1 physical pixel.
+            const _dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+            const _pp = (v) => Math.round(v * _dpr) / _dpr;
+            const _ps = 1 / _dpr; // 1 physical pixel in CSS px units
+            const outerTickLength = typeof axis.tickLength === 'number' && Number.isFinite(axis.tickLength)
+                ? axis.tickLength
+                : 6;
+            const rightTickH = typeof axis.xTickLengthAtRightPx === 'number' &&
+                Number.isFinite(axis.xTickLengthAtRightPx) &&
+                axis.xTickLengthAtRightPx > 0
+                ? axis.xTickLengthAtRightPx
+                : outerTickLength;
+            const xAxisDomainExtendRight = typeof axis.xDomainExtendRightPx === 'number' &&
+                Number.isFinite(axis.xDomainExtendRightPx) &&
+                axis.xDomainExtendRightPx >= 0
+                ? axis.xDomainExtendRightPx
+                : 0;
+            // Remove D3 domain path; replace with a physical-pixel-perfect rect.
+            svgElement.selectAll('.domain').remove();
+            svgElement.selectAll('.gcharts-axis__domain-rect').remove();
+            // Use .style() so inline styles override any CSS class rules (fill/stroke from library CSS).
+            svgElement.append('rect')
+                .attr('class', 'gcharts-axis__domain-rect')
+                .attr('x', 0)
+                .attr('y', _pp(0))
+                .attr('width', Math.round(width) + xAxisDomainExtendRight)
+                .attr('height', _ps)
+                .attr('shape-rendering', 'crispEdges')
+                .style('fill', '#000000')
+                .style('stroke', 'none');
+            // Remove all D3 tick marks and library duplicates; replace with per-tick rects.
+            svgElement.selectAll('.tick line').remove();
+            svgElement.selectAll('.gcharts-axis__outer-tick-batch').remove();
+            svgElement.selectAll('.gcharts-axis__tick-rect').remove();
+            const outerTickXSeen = new Set();
+            const xBand = isBandScale(axisScale);
+            /** Right-edge stub (e.g. x=0 reverse): paint in plotAfter so Y-domain rect does not cover it. */
+            let cornerTickPlot = null;
+            svgElement.selectAll('.tick').each(function () {
+                const tick = select(this);
+                tick.selectAll('.gcharts-axis__outer-tick').remove();
+                tick.selectAll('path').remove();
+                const currentTransform = tick.attr('transform') || '';
+                const tMatch = currentTransform.match(/translate\(\s*([^,)\s]+)/);
+                if (tMatch) {
+                    const rawX = parseFloat(tMatch[1]);
+                    if (xBand) {
+                        tick.attr('transform', `translate(${_pp(rawX)}, 0)`);
+                        if (rawX > width + 2) {
+                            return;
+                        }
+                        svgElement.append('rect')
+                            .attr('class', 'gcharts-axis__tick-rect')
+                            .attr('x', _pp(rawX))
+                            .attr('y', 0)
+                            .attr('width', _ps)
+                            .attr('height', outerTickLength)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', '#000000')
+                            .style('stroke', 'none');
+                        return;
+                    }
+                    const tx = Math.round(rawX);
+                    tick.attr('transform', 'translate(' + tx + ', 0)');
+                    if (outerTickXSeen.has(tx)) {
+                        return;
+                    }
+                    outerTickXSeen.add(tx);
+                    if (tx > Math.round(width)) {
+                        return;
+                    }
+                    const axisRightX = Math.round(width);
+                    const atRightEdge = tx >= axisRightX - 1;
+                    const tickX = atRightEdge
+                        ? Math.max(0, _pp(axisRightX))
+                        : Math.min(_pp(tx), _pp(width) - _ps);
+                    const tickHeight = atRightEdge ? Math.max(rightTickH, _ps) : outerTickLength;
+                    if (atRightEdge) {
+                        cornerTickPlot = { x: Math.max(0, tickX), h: tickHeight };
+                    }
+                    else {
+                        svgElement.append('rect')
+                            .attr('class', 'gcharts-axis__tick-rect')
+                            .attr('x', Math.max(0, tickX))
+                            .attr('y', 0)
+                            .attr('width', _ps)
+                            .attr('height', tickHeight)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', '#000000')
+                            .style('stroke', 'none');
+                    }
+                }
+            });
+            if (!xBand && axis.omitCornerVerticalTick) {
+                const axisRightX = Math.round(width);
+                if (!outerTickXSeen.has(axisRightX) && !cornerTickPlot) {
+                    cornerTickPlot = {
+                        x: Math.max(0, _pp(axisRightX)),
+                        h: Math.max(rightTickH, _ps),
+                    };
+                }
+            }
             // add an axis header if necessary
             if (axis.title.text) {
                 const titleRows = await getAxisTitleRows({ axis, textMaxWidth: width });
+                if (cancelled || !ref.current) {
+                    return;
+                }
                 const titleClassName = b('title');
                 svgElement.selectAll(`.${titleClassName}`).remove();
                 svgElement
@@ -89,6 +207,9 @@ export const AxisX = React.memo(function AxisX(props) {
                     return `translate(${x}, ${y})`;
                 })
                     .attr('font-size', axis.title.style.fontSize)
+                    .attr('font-weight', axis.title.style.fontWeight || '500')
+                    .attr('fill', '#000000')
+                    .attr('text-rendering', 'geometricPrecision')
                     .attr('text-anchor', 'middle')
                     .selectAll('tspan')
                     .data(titleRows)
@@ -184,7 +305,7 @@ export const AxisX = React.memo(function AxisX(props) {
                         const plotLineValue = Number(axisScale(plotLine.value));
                         const points = [
                             [plotLineValue, 0],
-                            [plotLineValue, totalHeight],
+                            [plotLineValue, plotBottomY],
                         ];
                         itemSelection
                             .append('path')
@@ -209,7 +330,50 @@ export const AxisX = React.memo(function AxisX(props) {
                 setPlotLines(plotBeforeContainer, axis.plotLines.filter((d) => d.layerPlacement === 'before'));
                 setPlotLines(plotAfterContainer, axis.plotLines.filter((d) => d.layerPlacement === 'after'));
             }
+            if (cornerTickPlot) {
+                const corner = cornerTickPlot;
+                const paintCornerTick = () => {
+                    if (cancelled) {
+                        return;
+                    }
+                    if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
+                        const pa = select(plotAfterRef.current);
+                        pa.selectAll('[data-gcharts-x-corner-tick]').remove();
+                        pa.append('rect')
+                            .attr('data-gcharts-x-corner-tick', '1')
+                            .attr('class', 'gcharts-axis__tick-rect')
+                            .attr('x', corner.x)
+                            .attr('y', plotBottomY)
+                            .attr('width', _ps)
+                            .attr('height', corner.h)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', '#000000')
+                            .style('stroke', 'none');
+                    }
+                    else {
+                        svgElement.append('rect')
+                            .attr('class', 'gcharts-axis__tick-rect')
+                            .attr('x', corner.x)
+                            .attr('y', 0)
+                            .attr('width', _ps)
+                            .attr('height', corner.h)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', '#000000')
+                            .style('stroke', 'none');
+                    }
+                };
+                queueMicrotask(paintCornerTick);
+            }
         })();
+        return () => {
+            cancelled = true;
+            if (ref.current) {
+                select(ref.current).selectAll('*').remove();
+            }
+            if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
+                select(plotAfterRef.current).selectAll('[data-gcharts-x-corner-tick]').remove();
+            }
+        };
     }, [
         axis,
         boundsOffsetLeft,
@@ -222,6 +386,7 @@ export const AxisX = React.memo(function AxisX(props) {
         split,
         totalHeight,
         width,
+        xAxisBaselineY,
     ]);
     return React.createElement("g", { ref: ref });
 });
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisX/styles.css b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisX/styles.css
index 5685097..7c6bac4 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisX/styles.css
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisX/styles.css
@@ -1,16 +1,30 @@
 .gcharts-axis .domain {
-  stroke: var(--g-color-line-generic-active);
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
 }
 .gcharts-axis .tick text {
-  color: var(--g-color-text-secondary);
+  color: #000000;
+  fill: #000000;
   dominant-baseline: text-after-edge;
+  text-rendering: optimizeSpeed;
+  font-weight: 400;
 }
-.gcharts-axis .tick line, .gcharts-axis .tick path {
-  stroke: var(--g-color-line-generic);
+.gcharts-axis .gcharts-axis__outer-tick {
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
+}
+.gcharts-axis .gcharts-axis__outer-tick-batch {
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
 }
 .gcharts-axis__title {
   dominant-baseline: text-after-edge;
-  fill: var(--g-color-text-secondary);
+  fill: #000000;
+  text-rendering: optimizeSpeed;
+  font-weight: 400;
 }
 .gcharts-axis__title tspan {
   dominant-baseline: text-after-edge;
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/AxisY.js b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/AxisY.js
index c0e99ce..2920362 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/AxisY.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/AxisY.js
@@ -25,30 +25,91 @@ export const AxisY = (props) => {
         if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
             plotAfterContainer = select(plotAfterRef.current);
             plotAfterContainer.selectAll(`[${plotDataAttr}]`).remove();
+            plotAfterContainer.selectAll('[data-y-outer-ticks]').remove();
+            plotAfterContainer.selectAll('[data-y-domain-overlay]').remove();
         }
         if (preparedAxisData.title) {
-            svgElement
+            const tt = preparedAxisData.title;
+            const titleTextSelection = svgElement
                 .append('g')
                 .attr('class', b('title'))
+                .attr('transform', `translate(${Math.round(tt.x)}, ${Math.round(tt.y)}) rotate(${tt.rotate}) translate(0, ${Math.round(tt.offset)})`)
                 .append('text')
                 .attr('text-anchor', 'start')
-                .style('dominant-baseline', 'text-after-edge')
-                .style('transform', `translate(${preparedAxisData.title.x}px, ${preparedAxisData.title.y}px) rotate(${preparedAxisData.title.rotate}deg) translate(0px, ${preparedAxisData.title.offset}px)`)
-                .attr('font-size', preparedAxisData.title.style.fontSize)
-                .selectAll('tspan')
-                .data(preparedAxisData.title.content)
-                .join('tspan')
-                .html((d) => d.text)
-                .attr('x', (d) => d.x)
-                .attr('y', (d) => d.y)
-                .attr('text-anchor', 'start');
+                .style('dominant-baseline', 'central')
+                .attr('text-rendering', 'optimizeLegibility')
+                .attr('font-size', tt.style.fontSize)
+                .attr('font-weight', tt.style.fontWeight || '500')
+                .attr('fill', '#000000')
+                .style('letter-spacing', (tt.style && tt.style.letterSpacing) || 'normal');
+            if (tt.content.length === 1) {
+                const row = tt.content[0];
+                titleTextSelection
+                    .text(row.text)
+                    .attr('x', row.x)
+                    .attr('y', row.y);
+            }
+            else {
+                titleTextSelection
+                    .selectAll('tspan')
+                    .data(tt.content)
+                    .join('tspan')
+                    .text((d) => d.text)
+                    .attr('x', (d) => (d.x != null ? d.x : null))
+                    .attr('y', (d) => (d.y != null ? d.y : null))
+                    .attr('dx', (d) => (d.dx != null ? d.dx : null))
+                    .attr('dy', (d) => (d.dy != null ? d.dy : null))
+                    .attr('text-anchor', 'start');
+            }
+        }
+        // Compute tick Y extents first so the domain line aligns exactly with actual tick positions.
+        let bottomMostTickY = -Infinity;
+        let topMostTickY = Infinity;
+        for (const t of preparedAxisData.ticks) {
+            const ty = t.line ? t.line.points[0][1] : (t.svgLabel ? t.svgLabel.y : null);
+            if (ty !== null) {
+                if (ty > bottomMostTickY) {
+                    bottomMostTickY = ty;
+                }
+                if (ty < topMostTickY) {
+                    topMostTickY = ty;
+                }
+            }
         }
+        const domainX = preparedAxisData.domain
+            ? Math.round(preparedAxisData.domain.start[0])
+            : null;
+        // Use <rect fill> instead of <path stroke> for all axis lines.
+        // A stroked path at integer coordinates straddles pixel boundaries and renders as
+        // 2 physical pixels at DPR≠1 (e.g. Windows 125% scaling = DPR 1.25).
+        // A filled <rect> at physical-pixel-aligned coordinates is guaranteed 1 physical pixel.
+        const _dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+        const _pp = (v) => Math.round(v * _dpr) / _dpr;
+        const _ps = 1 / _dpr; // 1 physical pixel in CSS px units
         if (preparedAxisData.domain) {
-            svgElement
-                .append('path')
-                .attr('class', b('domain'))
-                .attr('d', lineGenerator([preparedAxisData.domain.start, preparedAxisData.domain.end]))
-                .style('stroke', preparedAxisData.domain.lineColor);
+            // dStartY = bottom end of Y-axis; no extra "foot" below y=0,
+            // чтобы в углу (0,0) не рисовался второй вертикальный штрих.
+            // dEndY   = top end of Y-axis (smallest SVG Y = highest on screen) = exactly at topmost tick
+            const dStartY = bottomMostTickY > -Infinity
+                ? _pp(Math.round(bottomMostTickY))
+                : _pp(Math.round(preparedAxisData.domain.end[1]));
+            const dEndY = topMostTickY < Infinity
+                ? _pp(Math.round(topMostTickY))
+                : _pp(Math.round(preparedAxisData.domain.start[1]));
+            const domainContainer = plotAfterContainer || svgElement;
+            // Physical-pixel-wide vertical rect for the Y-axis domain line.
+            // Use .style() so inline styles override any CSS class rules.
+            domainContainer
+                .append('rect')
+                .attr('class', 'gcharts-y-axis__domain-rect')
+                .attr('data-y-domain-overlay', 1)
+                .attr('x', _pp(domainX))
+                .attr('y', dEndY)
+                .attr('width', _ps)
+                .attr('height', dStartY - dEndY)
+                .attr('shape-rendering', 'crispEdges')
+                .style('fill', '#000000')
+                .style('stroke', 'none');
         }
         const tickClassName = b('tick');
         const ticks = svgElement
@@ -58,12 +119,61 @@ export const AxisY = (props) => {
             .join('g')
             .attr('class', tickClassName);
         const labelClassName = b('label');
+        const tickLength = typeof preparedAxisData.tickLength === 'number' &&
+            Number.isFinite(preparedAxisData.tickLength) &&
+            preparedAxisData.tickLength > 0
+            ? preparedAxisData.tickLength
+            : 8;
+        const bottomTickLength = typeof preparedAxisData.yTickLengthAtBottomPx === 'number' &&
+            Number.isFinite(preparedAxisData.yTickLengthAtBottomPx) &&
+            preparedAxisData.yTickLengthAtBottomPx > 0
+            ? preparedAxisData.yTickLengthAtBottomPx
+            : tickLength;
+        const outerTickSegments = [];
+        const outerTickYSeen = new Set();
         ticks.each(function () {
             var _a;
             const tickSelection = select(this);
             const tickData = tickSelection.datum();
-            if (tickData.line) {
-                tickSelection.append('path').attr('d', lineGenerator(tickData.line.points));
+            const rawTickY = tickData.line
+                ? tickData.line.points[0][1]
+                : (tickData.svgLabel ? tickData.svgLabel.y : 0);
+            const roundedTickY = Math.round(rawTickY);
+            const isBottomTick = bottomMostTickY > -Infinity && Math.abs(rawTickY - bottomMostTickY) < 1;
+            if (tickData.line && !isBottomTick) {
+                const xLeft = tickData.line.points[0][0];
+                const xRight = tickData.line.points[1][0];
+                let gridX0 = xLeft;
+                let gridX1 = xRight;
+                if (domainX !== null) {
+                    if (xRight <= domainX) {
+                        gridX1 = xRight;
+                    } else {
+                        gridX0 = Math.max(xLeft, domainX + tickLength);
+                    }
+                }
+                if (gridX1 > gridX0) {
+                    // Physical-pixel-tall horizontal rect for each grid line.
+                    tickSelection.append('rect')
+                        .attr('class', 'gcharts-y-axis__grid-seg-rect')
+                        .attr('x', gridX0)
+                        .attr('y', _pp(roundedTickY))
+                        .attr('width', gridX1 - gridX0)
+                        .attr('height', _ps)
+                        .attr('shape-rendering', 'crispEdges')
+                        .style('fill', 'var(--g-color-line-generic)')
+                        .style('stroke', 'none');
+                }
+            }
+            if (domainX !== null) {
+                // Show outer tick mark for ALL Y-axis values including the bottommost (y=0).
+                // The grid line at y=0 is still skipped (it would overlap the X-axis domain),
+                // but the horizontal dash on the Y-axis itself must be visible at every value.
+                const endX = domainX + (isBottomTick ? bottomTickLength : tickLength);
+                if (!outerTickYSeen.has(roundedTickY)) {
+                    outerTickYSeen.add(roundedTickY);
+                    outerTickSegments.push({ tickStartX: domainX, endX, roundedTickY });
+                }
             }
             if (tickData.svgLabel) {
                 const label = tickData.svgLabel;
@@ -89,6 +199,7 @@ export const AxisY = (props) => {
                     .attr('class', labelClassName)
                     .style('dominant-baseline', 'text-before-edge')
                     .style('font-size', label.style.fontSize)
+                    .style('font-weight', label.style.fontWeight || '400')
                     .style('fill', (_a = label.style.fontColor) !== null && _a !== void 0 ? _a : '');
             }
         });
@@ -147,13 +258,39 @@ export const AxisY = (props) => {
                     .attr(plotDataAttr, 1)
                     .attr(plotLineDataAttr, 1)
                     .style('transform', (d) => `translate(${d.x}px, ${d.y}px)`);
-                plotLinesSelection
-                    .append('path')
-                    .attr('d', (d) => lineGenerator(d.points))
-                    .attr('stroke', (d) => d.color)
-                    .attr('stroke-width', (d) => d.lineWidth)
-                    .attr('stroke-dasharray', (d) => getLineDashArray(d.dashStyle, d.lineWidth))
-                    .attr('opacity', (d) => d.opacity);
+                plotLinesSelection.each(function () {
+                    const itemSelection = select(this);
+                    const plotLine = itemSelection.datum();
+                    const points = plotLine.points || [];
+                    const isHorizontal = points.length >= 2 && points.every((p) => p[1] === points[0][1]);
+                    if (isHorizontal) {
+                        const rawY = points[0][1];
+                        const x0 = Math.min(points[0][0], points[points.length - 1][0]);
+                        const x1 = Math.max(points[0][0], points[points.length - 1][0]);
+                        itemSelection
+                            .append('rect')
+                            .attr('x', x0)
+                            .attr('y', _pp(rawY))
+                            .attr('width', Math.max(0, x1 - x0))
+                            .attr('height', _ps)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', plotLine.color)
+                            .style('stroke', 'none')
+                            .style('opacity', plotLine.opacity);
+                    }
+                    else {
+                        itemSelection
+                            .append('path')
+                            .attr('d', lineGenerator(plotLine.points))
+                            .attr('stroke', plotLine.color)
+                            .attr('stroke-width', plotLine.lineWidth)
+                            .attr('stroke-dasharray', getLineDashArray(plotLine.dashStyle, plotLine.lineWidth))
+                            .style('stroke-linecap', 'butt')
+                            .style('shape-rendering', 'geometricPrecision')
+                            .attr('fill', 'none')
+                            .attr('opacity', plotLine.opacity);
+                    }
+                });
                 plotLinesSelection.each(function () {
                     var _a, _b;
                     const itemSelection = select(this);
@@ -175,8 +312,58 @@ export const AxisY = (props) => {
             setPlotLines(plotBeforeContainer, preparedAxisData.plotLines.filter((item) => item.layerPlacement === 'before'));
             setPlotLines(plotAfterContainer, preparedAxisData.plotLines.filter((item) => item.layerPlacement === 'after'));
         }
+        if (outerTickSegments.length) {
+            const appendOuterTicks = (container) => {
+                if (!container) {
+                    return;
+                }
+                const layer = container
+                    .append('g')
+                    .attr('data-y-outer-ticks', preparedAxisData.id)
+                    .attr('class', 'gcharts-y-axis__outer-ticks-overlay');
+                // Physical-pixel-tall horizontal rects for Y-axis outer tick marks.
+                outerTickSegments.forEach((seg) => {
+                    layer.append('rect')
+                        .attr('class', 'gcharts-y-axis__outer-tick-rect')
+                        .attr('x', seg.tickStartX)
+                        .attr('y', _pp(seg.roundedTickY))
+                        .attr('width', seg.endX - seg.tickStartX)
+                        .attr('height', _ps)
+                        .attr('shape-rendering', 'crispEdges')
+                        .style('fill', '#000000')
+                        .style('stroke', 'none');
+                });
+            };
+            if (plotAfterContainer) {
+                appendOuterTicks(plotAfterContainer);
+            }
+            else {
+                outerTickSegments.forEach((seg) => {
+                    svgElement.append('rect')
+                        .attr('class', 'gcharts-y-axis__outer-tick-rect')
+                        .attr('x', seg.tickStartX)
+                        .attr('y', _pp(seg.roundedTickY))
+                        .attr('width', seg.endX - seg.tickStartX)
+                        .attr('height', _ps)
+                        .attr('shape-rendering', 'crispEdges')
+                        .style('fill', '#000000')
+                        .style('stroke', 'none');
+                });
+            }
+        }
+        return () => {
+            if (ref.current) {
+                select(ref.current).selectAll('*').remove();
+            }
+            if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
+                const after = select(plotAfterRef.current);
+                after.selectAll('[data-y-outer-ticks]').remove();
+                after.selectAll('[data-y-domain-overlay]').remove();
+            }
+        };
     }, [lineGenerator, plotAfterRef, plotBeforeRef, preparedAxisData]);
     return (React.createElement(React.Fragment, null,
         React.createElement(HtmlLayer, { preparedData: { htmlElements: htmlLabels }, htmlLayout: htmlLayout }),
         React.createElement("g", { ref: ref, className: b() })));
 };
+
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/prepare-axis-data.js b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/prepare-axis-data.js
index 052af4f..ee6cc06 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/prepare-axis-data.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/prepare-axis-data.js
@@ -95,7 +95,7 @@ async function getSvgAxisLabel({ getTextSize, text, axis, top, left, labelMaxHei
         });
     }
     const x = axis.position === 'left' ? left - axis.labels.margin : left + axis.labels.margin;
-    const y = top;
+    const y = top + (typeof axis.labels.y === 'number' ? axis.labels.y : 0);
     const svgLabel = {
         title: content.length > 1 || ((_a = content[0]) === null || _a === void 0 ? void 0 : _a.text) !== text ? text : undefined,
         content: content,
@@ -117,7 +117,7 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
     if (axis.visible) {
         domain = {
             start: [domainX, axisPlotTopPosition],
-            end: [domainX, axisPlotTopPosition + axisHeight],
+            end: [domainX, axisPlotTopPosition + axisHeight + 2],
             lineColor: (_c = axis.lineColor) !== null && _c !== void 0 ? _c : '',
         };
     }
@@ -127,9 +127,15 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
     const values = getTickValues({ scale, axis, labelLineHeight, series });
     const tickStep = getMinSpaceBetween(values, (d) => Number(d.value));
     const labelMaxHeight = values.length > 1 ? values[0].y - values[1].y - axis.labels.padding * 2 : axisHeight;
+    // Same snap as AxisY grid rects (_pp ∘ Math.round) so labels + tick paths match physical pixels.
+    const dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+    const snapTickY = (yFloat) => {
+        const r = Math.round(yFloat);
+        return Math.round(r * dpr) / dpr;
+    };
     for (let i = 0; i < values.length; i++) {
         const tickValue = values[i];
-        const y = axisPlotTopPosition + tickValue.y;
+        const y = snapTickY(axisPlotTopPosition + tickValue.y);
         let svgLabel = null;
         let htmlLabel = null;
         if (axis.labels.enabled) {
@@ -149,7 +155,7 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
                 htmlLabel = {
                     content,
                     x,
-                    y: top - size.height / 2,
+                    y: top - size.height / 2 + (typeof axis.labels.y === 'number' ? axis.labels.y : 0),
                     size,
                     style: axis.labels.style,
                 };
@@ -181,6 +187,25 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
             htmlLabel,
         });
     }
+    {
+        const seenRoundY = new Set();
+        const uniqueTicks = [];
+        for (const t of ticks) {
+            const rawY = t.line ? t.line.points[0][1] : (t.svgLabel ? t.svgLabel.y : undefined);
+            if (rawY === undefined) {
+                uniqueTicks.push(t);
+                continue;
+            }
+            const k = Math.round(rawY);
+            if (seenRoundY.has(k)) {
+                continue;
+            }
+            seenRoundY.add(k);
+            uniqueTicks.push(t);
+        }
+        ticks.length = 0;
+        ticks.push(...uniqueTicks);
+    }
     let labelsWidth = ticks.reduce((acc, item) => { var _a, _b, _c, _d; return Math.max(acc, (_d = (_b = (_a = item.svgLabel) === null || _a === void 0 ? void 0 : _a.size.width) !== null && _b !== void 0 ? _b : (_c = item.htmlLabel) === null || _c === void 0 ? void 0 : _c.size.width) !== null && _d !== void 0 ? _d : 0); }, 0);
     labelsWidth = Math.min(axis.labels.width, labelsWidth);
     let title = null;
@@ -191,6 +216,8 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
         const cos = Math.abs(calculateCos(rotateAngle));
         const titleContent = [];
         const titleMaxWidth = sin * axisHeight;
+        let passengerInlineTspans = false;
+        let passengerTitleBox = null;
         if (axis.title.maxRowCount > 1) {
             const titleTextRows = await wrapText({
                 text: axis.title.text,
@@ -210,6 +237,54 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
                 });
             }
         }
+        else if (axis.title.text.replace(/\s+/g, ' ').trim() === 'Число пассажиров') {
+            const fsStr = String((axis.title.style && axis.title.style.fontSize) || '13px');
+            const fontSizePx = parseFloat(fsStr.replace(/^([\d.]+).*$/, '$1')) || 13;
+            const widenCheIPx = fontSizePx * 0.03;
+            const trackISCpx = fontSizePx * 0.026;
+            const bumpLoPx = 0;
+            const pullOxPx = -fontSizePx * 0.024;
+            const trackPassePx = fontSizePx * 0.021;
+            const bumpZhePx = fontSizePx * 0.036;
+            const trackIroPx = fontSizePx * 0.006;
+            const trackRoInZhirovPx = -fontSizePx * 0.01;
+            const tightPassejPx = -fontSizePx * 0.006;
+            const parts = [
+                { text: 'Ч', dx: 0 },
+                { text: 'и', dx: widenCheIPx },
+                { text: 'с', dx: trackISCpx },
+                { text: 'л', dx: bumpLoPx },
+                { text: 'о', dx: pullOxPx },
+                { text: ' ', dx: 0 },
+                { text: 'п', dx: 0 },
+                { text: 'а', dx: trackPassePx },
+                { text: 'с', dx: trackPassePx },
+                { text: 'с', dx: trackPassePx },
+                { text: 'а', dx: trackPassePx },
+                { text: 'ж', dx: bumpZhePx },
+                { text: 'и', dx: tightPassejPx },
+                { text: 'р', dx: trackIroPx },
+                { text: 'о', dx: trackRoInZhirovPx },
+                { text: 'в', dx: trackIroPx },
+            ];
+            let totalW = 0;
+            let maxH = 0;
+            for (let pi = 0; pi < parts.length; pi++) {
+                const p = parts[pi];
+                const sz = await getTitleTextSize(p.text);
+                totalW += p.dx + sz.width;
+                maxH = Math.max(maxH, sz.height);
+                titleContent.push({
+                    text: p.text,
+                    x: pi === 0 ? 0 : undefined,
+                    y: pi === 0 ? 0 : undefined,
+                    dx: typeof p.dx === 'number' && p.dx !== 0 ? p.dx : undefined,
+                    size: sz,
+                });
+            }
+            passengerInlineTspans = true;
+            passengerTitleBox = { width: totalW, height: maxH };
+        }
         else {
             const text = await getTextWithElipsis({
                 text: axis.title.text,
@@ -223,11 +298,12 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
                 size: await getTitleTextSize(text),
             });
         }
-        const originalTextSize = titleContent.reduce((acc, item) => {
-            acc.width = Math.max(acc.width, item.size.width);
-            acc.height += item.size.height;
-            return acc;
-        }, { width: 0, height: 0 });
+        const originalTextSize = passengerTitleBox ||
+            titleContent.reduce((acc, item) => {
+                acc.width = Math.max(acc.width, item.size.width);
+                acc.height += item.size.height;
+                return acc;
+            }, { width: 0, height: 0 });
         const rotatedTitleSize = {
             width: sin * originalTextSize.height + cos * originalTextSize.width,
             height: sin * originalTextSize.width + cos * originalTextSize.height,
@@ -252,6 +328,9 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
         const x = axis.position === 'left'
             ? -left - labelsWidth - axis.labels.margin - axis.title.margin
             : -left + width + labelsWidth + axis.labels.margin + axis.title.margin;
+        const titleOffset = passengerInlineTspans
+            ? 0
+            : -(originalTextSize.height / titleContent.length) * (titleContent.length - 1);
         title = {
             content: titleContent,
             style: axis.title.style,
@@ -259,7 +338,7 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
             x,
             y: axisPlotTopPosition + y,
             rotate: rotateAngle,
-            offset: -(originalTextSize.height / titleContent.length) * (titleContent.length - 1),
+            offset: titleOffset,
         };
     }
     const plotBands = [];
@@ -326,6 +405,23 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
             dashStyle: plotLine.dashStyle,
         });
     }
+    if (domain && ticks.length > 0) {
+        let topTickY = Infinity;
+        let bottomTickY = -Infinity;
+        for (const t of ticks) {
+            const ty = t.line ? t.line.points[0][1] : (t.svgLabel ? t.svgLabel.y : null);
+            if (ty !== null) {
+                topTickY = Math.min(topTickY, ty);
+                bottomTickY = Math.max(bottomTickY, ty);
+            }
+        }
+        if (topTickY !== Infinity) {
+            domain.start = [domain.start[0], topTickY];
+        }
+        if (bottomTickY !== -Infinity) {
+            domain.end = [domain.end[0], bottomTickY + 6];
+        }
+    }
     return {
         id: getUniqId(),
         title,
@@ -333,5 +429,9 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
         domain,
         plotBands,
         plotLines,
+        tickLength: typeof axis.tickLength === 'number' ? axis.tickLength : undefined,
+        yTickLengthAtBottomPx: typeof axis.yTickLengthAtBottomPx === 'number'
+            ? axis.yTickLengthAtBottomPx
+            : undefined,
     };
 }
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/styles.css b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/styles.css
index ae33c44..b3bdbf6 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/styles.css
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/styles.css
@@ -1,15 +1,35 @@
 .gcharts-y-axis__domain {
-  stroke: var(--g-color-line-generic-active);
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
 }
 .gcharts-y-axis__label {
-  fill: var(--g-color-text-secondary);
+  fill: #000000;
   stroke: none;
   dominant-baseline: text-after-edge;
+  text-rendering: optimizeSpeed;
+  font-weight: 400;
 }
 .gcharts-y-axis__tick {
-  stroke: var(--g-color-line-generic);
+  stroke: none;
+  shape-rendering: crispEdges;
 }
-.gcharts-y-axis__title {
-  dominant-baseline: text-after-edge;
-  fill: var(--g-color-text-secondary);
+.gcharts-y-axis__tick path {
+  stroke-width: 1;
+  shape-rendering: crispEdges;
+}
+.gcharts-y-axis__outer-tick {
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
+}
+.gcharts-y-axis__title text {
+  fill: #000000;
+  stroke: none;
+  text-rendering: optimizeSpeed;
+  font-weight: 400;
+}
+.gcharts-y-axis__title tspan {
+  fill: #000000;
+  stroke: none;
 }
\ No newline at end of file
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/utils.js b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/utils.js
index 0dd0e7b..dcef278 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/utils.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/AxisY/utils.js
@@ -1,3 +1,4 @@
+import get from 'lodash/get';
 import { getDomainDataYBySeries, getMinSpaceBetween, getTicksCount, isBandScale } from '../../utils';
 function thinOut(items, delta) {
     const arr = [];
@@ -7,6 +8,32 @@ function thinOut(items, delta) {
     return arr;
 }
 export function getTickValues({ scale, axis, labelLineHeight, series, }) {
+    const ensureZeroTick = (items) => {
+        if (!items.length || axis.min !== 0) {
+            return items;
+        }
+        const hasZero = items.some((i) => Number(i.value) === 0);
+        if (hasZero) {
+            return items;
+        }
+        const zeroY = scale(0);
+        if (!Number.isFinite(zeroY)) {
+            return items;
+        }
+        const next = [...items, { y: zeroY, value: 0 }];
+        next.sort((a, b) => Number(a.value) - Number(b.value));
+        return next;
+    };
+    var fixedTicks = get(axis, 'tickPositions');
+    if (Array.isArray(fixedTicks) && fixedTicks.length && scale && typeof scale === 'function') {
+        var items = fixedTicks.map(function (t) {
+            return ({
+                y: scale(Number(t)),
+                value: Number(t),
+            });
+        });
+        return ensureZeroTick(items);
+    }
     if ('ticks' in scale && typeof scale.ticks === 'function') {
         const range = scale.range();
         const height = Math.abs(range[0] - range[1]);
@@ -64,7 +91,7 @@ export function getTickValues({ scale, axis, labelLineHeight, series, }) {
                 }
             }
         }
-        return result;
+        return ensureZeroTick(result);
     }
     if (isBandScale(scale)) {
         const domain = scale.domain();
@@ -89,7 +116,7 @@ export function getTickValues({ scale, axis, labelLineHeight, series, }) {
                 availableSpaceForLabel = result[0].y - result[1].y - axis.labels.padding * 2;
             }
         }
-        return result;
+        return ensureZeroTick(result);
     }
     return [];
 }
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/ChartInner/index.js b/node_modules/@gravity-ui/charts/dist/cjs/components/ChartInner/index.js
index 11c5982..83b25e9 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/ChartInner/index.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/ChartInner/index.js
@@ -116,6 +116,41 @@ export const ChartInner = (props) => {
         return items;
     }, [boundsHeight, boundsOffsetTop, boundsWidth, preparedSeries, preparedSplit, yAxis, yScale]);
     const yAxisDataItems = useAsyncState([], setYAxisDataItems);
+    // X-axis group was translate(0, boundsHeight); Y grid/ticks use snapTickY(plot.top + scale(domainMin))
+    // from prepare-axis-data. If those differ (D3 float / DPR snap), the baseline sits off the y=0 row.
+    const axisXTranslateY = React.useMemo(() => {
+        var _e;
+        const plot0 = ((_e = preparedSplit === null || preparedSplit === void 0 ? void 0 : preparedSplit.plots) === null || _e === void 0 ? void 0 : _e[0]) || { top: 0, height: boundsHeight };
+        const top = plot0.top || 0;
+        const plotH = typeof plot0.height === 'number' ? plot0.height : boundsHeight;
+        const dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+        const snapY = (raw) => {
+            const r = Math.round(raw);
+            return Math.round(r * dpr) / dpr;
+        };
+        const yS = yScale === null || yScale === void 0 ? void 0 : yScale[0];
+        let rawBottom = top + plotH;
+        if (yS && typeof yS.domain === 'function') {
+            try {
+                const dom = yS.domain();
+                if (dom && dom.length) {
+                    const n0 = Number(dom[0]);
+                    const n1 = Number(dom[dom.length - 1]);
+                    if (Number.isFinite(n0) && Number.isFinite(n1)) {
+                        const domainMin = Math.min(n0, n1);
+                        const mapped = yS(domainMin);
+                        if (typeof mapped === 'number' && Number.isFinite(mapped)) {
+                            rawBottom = top + mapped;
+                        }
+                    }
+                }
+            }
+            catch (_err) {
+                rawBottom = top + plotH;
+            }
+        }
+        return snapY(rawBottom);
+    }, [boundsHeight, preparedSplit, yScale]);
     return (React.createElement("div", { className: b() },
         React.createElement("svg", { ref: svgRef, width: width, height: height,
             // We use onPointerMove here because onMouseMove works incorrectly when the zoom setting is enabled:
@@ -124,13 +159,21 @@ export const ChartInner = (props) => {
             React.createElement("defs", null,
                 React.createElement("clipPath", { id: clipPathId },
                     React.createElement("rect", { x: 0, y: 0, width: boundsWidth, height: boundsHeight }))),
-            title && React.createElement(Title, Object.assign({}, title, { chartWidth: width })),
+            title &&
+                React.createElement(Title, Object.assign({}, title, {
+                    chartWidth: boundsWidth +
+                        (((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.position) === 'right'
+                            ? ((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.panelWidth) || 0) + ((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.margin) || 0)
+                            : 0)),
+                    chartOffsetLeft: boundsOffsetLeft + 54,
+                    titleOffsetTop: 4,
+                })),
             React.createElement("g", { transform: `translate(0, ${boundsOffsetTop})` }, preparedSplit.plots.map((plot, index) => {
                 return React.createElement(PlotTitle, { key: `plot-${index}`, title: plot.title });
             })),
             React.createElement("g", { width: boundsWidth, height: boundsHeight, transform: `translate(${[boundsOffsetLeft, boundsOffsetTop].join(',')})`, ref: plotRef },
-                xScale && xAxis && (React.createElement("g", { transform: `translate(0, ${boundsHeight})` },
-                    React.createElement(AxisX, { axis: xAxis, boundsOffsetLeft: boundsOffsetLeft, boundsOffsetTop: boundsOffsetTop, height: boundsHeight, htmlLayout: htmlLayout, leftmostLimit: svgXPos, plotAfterRef: plotAfterRef, plotBeforeRef: plotBeforeRef, scale: xScale, split: preparedSplit, width: boundsWidth }))),
+                xScale && xAxis && (React.createElement("g", { transform: `translate(0, ${axisXTranslateY})` },
+                    React.createElement(AxisX, { axis: xAxis, boundsOffsetLeft: boundsOffsetLeft, boundsOffsetTop: boundsOffsetTop, height: boundsHeight, htmlLayout: htmlLayout, leftmostLimit: svgXPos, plotAfterRef: plotAfterRef, plotBeforeRef: plotBeforeRef, scale: xScale, split: preparedSplit, width: boundsWidth, xAxisBaselineY: axisXTranslateY }))),
                 Boolean(yAxisDataItems.length) && (React.createElement(React.Fragment, null, yAxisDataItems.map((axisData, index) => {
                     if (!axisData) {
                         return null;
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/ChartInner/useChartInnerProps.js b/node_modules/@gravity-ui/charts/dist/cjs/components/ChartInner/useChartInnerProps.js
index cdf130d..da93c13 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/ChartInner/useChartInnerProps.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/ChartInner/useChartInnerProps.js
@@ -125,9 +125,9 @@ export function useChartInnerProps(props) {
         yAxis,
         yScale,
     });
-    const boundsOffsetTop = chart.margin.top;
+    const boundsOffsetTop = Math.round(chart.margin.top);
     // We need to calculate the width of each left axis because the first axis can be hidden
-    const boundsOffsetLeft = chart.margin.left +
+    const boundsOffsetLeft = Math.round(chart.margin.left +
         yAxis.reduce((acc, axis) => {
             if (axis.position !== 'left') {
                 return acc;
@@ -137,7 +137,7 @@ export function useChartInnerProps(props) {
                 acc = axisWidth;
             }
             return acc;
-        }, 0);
+        }, 0));
     const { x } = (_a = svgContainer === null || svgContainer === void 0 ? void 0 : svgContainer.getBoundingClientRect()) !== null && _a !== void 0 ? _a : {};
     return {
         svgXPos: x,
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/Legend/index.js b/node_modules/@gravity-ui/charts/dist/cjs/components/Legend/index.js
index ba17740..bf030e1 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/Legend/index.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/Legend/index.js
@@ -236,6 +236,7 @@ export const Legend = (props) => {
                             return ('name' in d && d.name);
                         })
                             .style('font-size', legend.itemStyle.fontSize)
+                            .style('fill', legend.itemStyle.fontColor || null)
                             .each((d, index, nodes) => {
                             if (d.overflowed) {
                                 handleOverflowingText(nodes[index], d.textWidth);
@@ -263,7 +264,7 @@ export const Legend = (props) => {
                             break;
                         }
                     }
-                    const top = legendLineHeights.reduce((acc, h) => acc + h, 0);
+                    const top = legendLineHeights.reduce((acc, h) => acc + h, 0) + legendLineHeights.length * 2;
                     legendLineHeights.push(legendLineHeight);
                     legendLine.attr('transform', `translate(${[left, top].join(',')})`);
                     htmlLegendLine === null || htmlLegendLine === void 0 ? void 0 : htmlLegendLine.style('transform', `translate(${left}px, ${top}px)`);
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/components/Title/index.js b/node_modules/@gravity-ui/charts/dist/cjs/components/Title/index.js
index f16e541..9d6117f 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/components/Title/index.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/components/Title/index.js
@@ -1,7 +1,7 @@
 import React from 'react';
 export const Title = (props) => {
-    const { chartWidth, text, height, style } = props;
-    return (React.createElement("text", { dx: chartWidth / 2, dy: height / 2, dominantBaseline: "middle", textAnchor: "middle", style: {
+    const { chartWidth, chartOffsetLeft, titleOffsetTop, text, height, style } = props;
+    return (React.createElement("text", { dx: (chartOffsetLeft || 0) + chartWidth / 2, dy: height / 2 + (titleOffsetTop || 0), dominantBaseline: "middle", textAnchor: "middle", style: {
             fill: style === null || style === void 0 ? void 0 : style.fontColor,
             fontSize: style === null || style === void 0 ? void 0 : style.fontSize,
             fontWeight: style === null || style === void 0 ? void 0 : style.fontWeight,
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useAxis/index.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useAxis/index.js
index 7e733e8..5d3af29 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useAxis/index.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useAxis/index.js
@@ -1,10 +1,48 @@
 import React from 'react';
 import isEqual from 'lodash/isEqual';
+import { isAxisRelatedSeries } from '../../utils';
 import { getPreparedXAxis } from '../useChartOptions/x-axis';
 import { getPreparedYAxis } from '../useChartOptions/y-axis';
+
+/**
+ * Должна совпадать с useChartDimensions/getBottomOffset + формула boundsHeight,
+ * иначе getPreparedYAxis измеряет подписи по одной высоте, а шкала/линии — по другой.
+ */
+function estimateBoundsHeightForYAxisPreparation(args) {
+    const { height, preparedChart, preparedLegend, preparedXAxis, preparedSeries } = args;
+    const hasAxisRelatedSeries = preparedSeries.some(isAxisRelatedSeries);
+    let bottomOffset = 0;
+    if ((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) &&
+        preparedLegend.position !== 'right') {
+        bottomOffset += preparedLegend.height + preparedLegend.margin;
+    }
+    if (!(preparedXAxis === null || preparedXAxis === void 0 ? void 0 : preparedXAxis.visible)) {
+        return Math.round(height - preparedChart.margin.top - preparedChart.margin.bottom - bottomOffset);
+    }
+    if (hasAxisRelatedSeries) {
+        if (preparedXAxis.title.text) {
+            bottomOffset += preparedXAxis.title.height + preparedXAxis.title.margin;
+        }
+        if (preparedXAxis.labels.enabled) {
+            bottomOffset += preparedXAxis.labels.margin + preparedXAxis.labels.height;
+        }
+    }
+    return Math.round(height - preparedChart.margin.top - preparedChart.margin.bottom - bottomOffset);
+}
+
 export function useAxis(props) {
-    const { boundsHeight, height, preparedChart, preparedLegend, preparedSeries, preparedSeriesOptions, width, xAxis, yAxis, } = props;
-    const [axesState, setValue] = React.useState({ xAxis: null, yAxis: [] });
+    const {
+        boundsHeight,
+        height,
+        preparedChart,
+        preparedLegend,
+        preparedSeries,
+        preparedSeriesOptions,
+        width,
+        xAxis,
+        yAxis,
+    } = props;
+    const [axesState, setValue] = React.useState({xAxis: null, yAxis: []});
     const axesStateRunRef = React.useRef(0);
     const prevAxesStateValue = React.useRef(axesState);
     const axesStateReady = React.useRef(false);
@@ -20,18 +58,16 @@ export function useAxis(props) {
                 seriesData,
                 seriesOptions: preparedSeriesOptions,
             });
-            let estimatedBoundsHeight = boundsHeight !== null && boundsHeight !== void 0 ? boundsHeight : height;
-            if (preparedXAxis && typeof boundsHeight !== 'number') {
-                estimatedBoundsHeight =
-                    height -
-                        (preparedXAxis.title.height +
-                            preparedXAxis.title.margin +
-                            preparedXAxis.labels.margin +
-                            preparedXAxis.labels.height +
-                            (preparedLegend ? preparedLegend.height + preparedLegend.margin : 0) +
-                            preparedChart.margin.top +
-                            preparedChart.margin.bottom);
-            }
+            const estimatedBoundsHeight =
+                typeof boundsHeight === 'number'
+                    ? boundsHeight
+                    : estimateBoundsHeightForYAxisPreparation({
+                          height,
+                          preparedChart,
+                          preparedLegend,
+                          preparedXAxis,
+                          preparedSeries,
+                      });
             const preparedYAxis = await getPreparedYAxis({
                 height,
                 boundsHeight: estimatedBoundsHeight,
@@ -39,7 +75,7 @@ export function useAxis(props) {
                 seriesData,
                 yAxis,
             });
-            const newStateValue = { xAxis: preparedXAxis, yAxis: preparedYAxis };
+            const newStateValue = {xAxis: preparedXAxis, yAxis: preparedYAxis};
             if (axesStateRunRef.current === currentRun) {
                 if (!isEqual(prevAxesStateValue.current, newStateValue)) {
                     setValue(newStateValue);
@@ -59,5 +95,5 @@ export function useAxis(props) {
         xAxis,
         yAxis,
     ]);
-    return axesStateReady.current ? axesState : { xAxis: null, yAxis: [] };
+    return axesStateReady.current ? axesState : {xAxis: null, yAxis: []};
 }
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useAxisScales/index.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useAxisScales/index.js
index 6f4194f..7a5bc73 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useAxisScales/index.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useAxisScales/index.js
@@ -1,5 +1,5 @@
 import React from 'react';
-import { extent, scaleBand, scaleLinear, scaleLog, scaleUtc } from 'd3';
+import { extent, scaleBand, scaleLinear, scaleLog, scalePoint, scaleUtc } from 'd3';
 import get from 'lodash/get';
 import { DEFAULT_AXIS_TYPE, SeriesType } from '../../constants';
 import { CHART_SERIES_WITH_VOLUME_ON_Y_AXIS, getAxisHeight, getDataCategoryValue, getDefaultMaxXAxisValue, getDefaultMinXAxisValue, getDomainDataXBySeries, getDomainDataYBySeries, getOnlyVisibleSeries, isAxisRelatedSeries, isSeriesWithCategoryValues, } from '../../utils';
@@ -26,11 +26,20 @@ function validateArrayData(data) {
 }
 function filterCategoriesByVisibleSeries(args) {
     const { axisDirection, categories, series } = args;
+    if (axisDirection === 'x') {
+        const axisSeries = series.filter(isAxisRelatedSeries);
+        if (axisSeries.length > 0 && axisSeries.every((s) => s.type === SeriesType.Line)) {
+            return categories;
+        }
+    }
     const visibleCategories = new Set();
     series.forEach((s) => {
         if (isSeriesWithCategoryValues(s)) {
             s.data.forEach((d) => {
-                visibleCategories.add(getDataCategoryValue({ axisDirection, categories, data: d }));
+                const val = getDataCategoryValue({ axisDirection, categories, data: d });
+                if (val !== null && val !== undefined) {
+                    visibleCategories.add(val);
+                }
             });
         }
     });
@@ -107,7 +116,14 @@ export function createYScale(args) {
                 }
                 const domainOffsetMin = Math.abs(scale.invert(offsetMin) - scale.invert(0));
                 const domainOffsetMax = Math.abs(scale.invert(offsetMax) - scale.invert(0));
-                return scale.domain([yMin - domainOffsetMin, yMax + domainOffsetMax]);
+                const effectiveMax = yMax + domainOffsetMax;
+                const niceHint = effectiveMax > 120 ? 3 : 7;
+                var skipNice = get(axis, 'fixedNumericDomain') === true ||
+                    (Array.isArray(get(axis, 'tickPositions')) && get(axis, 'tickPositions').length > 0);
+                if (skipNice) {
+                    return scale.domain([yMin - domainOffsetMin, effectiveMax]);
+                }
+                return scale.domain([yMin - domainOffsetMin, effectiveMax]).nice(niceHint);
             }
             break;
         }
@@ -278,6 +294,12 @@ export function createXScale(args) {
                 if (!hasZoomX && !hasOffset) {
                     // 10 is the default value for the number of ticks. Here, to preserve the appearance of a series with a small number of points
                     scale.nice(Math.max(10, domainData.length));
+                    // Restore explicitly set min/max that nice() may have rounded away
+                    const [nicedMin, nicedMax] = scale.domain();
+                    scale.domain([
+                        typeof xMinProps === 'number' ? xMinProps : nicedMin,
+                        typeof xMaxProps === 'number' ? xMaxProps : nicedMax,
+                    ]);
                 }
                 return scale;
             }
@@ -290,11 +312,22 @@ export function createXScale(args) {
                     categories: xCategories,
                     series: series,
                 });
-                const xScale = scaleBand().domain(filteredCategories).range([0, boundsWidth]);
-                if (xScale.step() / 2 < xAxisMaxPadding) {
-                    xScale.range(range);
+                const axisSeries = series.filter(isAxisRelatedSeries);
+                const pointScaleSeriesTypes = new Set([SeriesType.Line, SeriesType.Area]);
+                const useCategoryPointScale = filteredCategories.length >= 2 &&
+                    axisSeries.length > 0 &&
+                    axisSeries.every((s) => pointScaleSeriesTypes.has(s.type));
+                if (useCategoryPointScale) {
+                    return scalePoint()
+                        .domain(filteredCategories)
+                        .range([0, boundsWidth])
+                        .padding(0);
                 }
-                return xScale;
+                return scaleBand()
+                    .domain(filteredCategories)
+                    .range([0, boundsWidth])
+                    .paddingInner(0)
+                    .paddingOuter(0);
             }
             break;
         }
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartDimensions/index.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartDimensions/index.js
index 9bedeb9..a948dd0 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartDimensions/index.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartDimensions/index.js
@@ -5,7 +5,8 @@ export { getBoundsWidth } from './utils';
 const getBottomOffset = (args) => {
     const { hasAxisRelatedSeries, preparedLegend, preparedXAxis } = args;
     let result = 0;
-    if (preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) {
+    // Right-panel legend does not consume vertical space at the bottom
+    if ((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) && preparedLegend.position !== 'right') {
         result += preparedLegend.height + preparedLegend.margin;
     }
     if (!(preparedXAxis === null || preparedXAxis === void 0 ? void 0 : preparedXAxis.visible)) {
@@ -25,13 +26,19 @@ export const useChartDimensions = (args) => {
     const { margin, width, height, preparedLegend, preparedXAxis, preparedYAxis, preparedSeries } = args;
     return React.useMemo(() => {
         const hasAxisRelatedSeries = preparedSeries.some(isAxisRelatedSeries);
-        const boundsWidth = getBoundsWidth({ chartWidth: width, chartMargin: margin, preparedYAxis });
+        // When legend is a right panel, subtract its width + margin from the plot area
+        const legendPanelReserve =
+            (preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) &&
+            preparedLegend.position === 'right'
+                ? preparedLegend.panelWidth + preparedLegend.margin
+                : 0;
+        const boundsWidth = getBoundsWidth({ chartWidth: width, chartMargin: margin, preparedYAxis }) - legendPanelReserve;
         const bottomOffset = getBottomOffset({
             hasAxisRelatedSeries,
             preparedLegend,
             preparedXAxis,
         });
-        const boundsHeight = height - margin.top - margin.bottom - bottomOffset;
-        return { boundsWidth, boundsHeight };
+        const boundsHeight = Math.round(height - margin.top - margin.bottom - bottomOffset);
+        return { boundsWidth: Math.round(boundsWidth), boundsHeight };
     }, [margin, width, height, preparedLegend, preparedXAxis, preparedYAxis, preparedSeries]);
 };
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartOptions/x-axis.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartOptions/x-axis.js
index b78b24a..9235baa 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartOptions/x-axis.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartOptions/x-axis.js
@@ -75,6 +75,7 @@ export const getPreparedXAxis = async ({ xAxis, seriesData, seriesOptions, width
     });
     const labelsStyle = {
         fontSize: get(xAxis, 'labels.style.fontSize', DEFAULT_AXIS_LABEL_FONT_SIZE),
+        fontWeight: get(xAxis, 'labels.style.fontWeight', '400'),
     };
     const labelsHtml = get(xAxis, 'labels.html', false);
     const labelsLineHeight = labelsHtml
@@ -90,6 +91,9 @@ export const getPreparedXAxis = async ({ xAxis, seriesData, seriesOptions, width
             dateFormat: get(xAxis, 'labels.dateFormat'),
             numberFormat: get(xAxis, 'labels.numberFormat'),
             rotation: get(xAxis, 'labels.rotation', 0),
+            align: get(xAxis, 'labels.align'),
+            x: get(xAxis, 'labels.x'),
+            y: get(xAxis, 'labels.y'),
             style: labelsStyle,
             width: 0,
             height: 0,
@@ -153,6 +157,17 @@ export const getPreparedXAxis = async ({ xAxis, seriesData, seriesOptions, width
         },
         visible: get(xAxis, 'visible', true),
         order: xAxis === null || xAxis === void 0 ? void 0 : xAxis.order,
+        tickLength: get(xAxis, 'tickLength'),
+        tickPosition: get(xAxis, 'tickPosition'),
+        lineWidth: get(xAxis, 'lineWidth'),
+        omitCornerVerticalTick: get(xAxis, 'omitCornerVerticalTick'),
+        zeroLabelVerticalNudgePx: get(xAxis, 'zeroLabelVerticalNudgePx'),
+        zeroLabelHNudgePx: get(xAxis, 'zeroLabelHNudgePx'),
+        xTickLengthAtRightPx: get(xAxis, 'xTickLengthAtRightPx'),
+        xDomainExtendRightPx: get(xAxis, 'xDomainExtendRightPx'),
+        startOnTick: get(xAxis, 'startOnTick'),
+        endOnTick: get(xAxis, 'endOnTick'),
+        minPadding: get(xAxis, 'minPadding'),
     };
     await setLabelSettings({
         axis: preparedXAxis,
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartOptions/y-axis.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartOptions/y-axis.js
index da84daf..5878eff 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartOptions/y-axis.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useChartOptions/y-axis.js
@@ -63,6 +63,7 @@ export const getPreparedYAxis = ({ height, boundsHeight, width, seriesData, yAxi
         const labelsEnabled = get(axisItem, 'labels.enabled', true);
         const labelsStyle = {
             fontSize: get(axisItem, 'labels.style.fontSize', DEFAULT_AXIS_LABEL_FONT_SIZE),
+            fontWeight: get(axisItem, 'labels.style.fontWeight', '400'),
         };
         const labelsHtml = get(axisItem, 'labels.html', false);
         const labelsLineHeight = labelsHtml
@@ -97,6 +98,7 @@ export const getPreparedYAxis = ({ height, boundsHeight, width, seriesData, yAxi
                 lineHeight: labelsLineHeight,
                 maxWidth: (_b = calculateNumericProperty({ base: width, value: (_a = axisItem.labels) === null || _a === void 0 ? void 0 : _a.maxWidth })) !== null && _b !== void 0 ? _b : axisLabelsDefaults.maxWidth,
                 html: labelsHtml,
+                y: get(axisItem, 'labels.y', 0),
             },
             lineColor: get(axisItem, 'lineColor'),
             categories: axisItem.categories,
@@ -112,6 +114,8 @@ export const getPreparedYAxis = ({ height, boundsHeight, width, seriesData, yAxi
             },
             min: (_c = get(axisItem, 'min')) !== null && _c !== void 0 ? _c : getDefaultMinYAxisValue(axisSeriesData),
             max: get(axisItem, 'max'),
+            tickPositions: get(axisItem, 'tickPositions'),
+            fixedNumericDomain: get(axisItem, 'fixedNumericDomain'),
             maxPadding: get(axisItem, 'maxPadding', getMaxPaddingBySeries({ series: axisSeriesData })),
             grid: {
                 enabled: shouldHideGrid
@@ -127,6 +131,8 @@ export const getPreparedYAxis = ({ height, boundsHeight, width, seriesData, yAxi
                     })
                     : (_g = axisItem.ticks) === null || _g === void 0 ? void 0 : _g.pixelInterval,
             },
+            tickLength: get(axisItem, 'tickLength'),
+            yTickLengthAtBottomPx: get(axisItem, 'yTickLengthAtBottomPx'),
             position: get(axisItem, 'position', defaultAxisPosition),
             plotIndex: get(axisItem, 'plotIndex', 0),
             plotLines: get(axisItem, 'plotLines', []).map((d) => ({
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/prepare-legend.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/prepare-legend.js
index 6ac1679..d153a34 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/prepare-legend.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/prepare-legend.js
@@ -49,9 +49,14 @@ export async function getPreparedLegend(args) {
         }
     }
     const legendWidth = get(legend, 'width', CONTINUOUS_LEGEND_SIZE.width);
+    const legendPosition = get(legend, 'position', 'bottom');
+    // For right panel, the panel width is: legend.width if set, else 160px
+    const legendPanelWidth = legendPosition === 'right' ? get(legend, 'width', 160) : legendWidth;
     return {
-        align: get(legend, 'align', legendDefaults.align),
-        justifyContent: get(legend, 'justifyContent', legendDefaults.justifyContent),
+        // Right panel: force left alignment so the SVG group starts at config.offset.left
+        align: legendPosition === 'right' ? 'left' : get(legend, 'align', legendDefaults.align),
+        // Right panel: force left-aligned items within the panel
+        justifyContent: legendPosition === 'right' ? 'start' : get(legend, 'justifyContent', legendDefaults.justifyContent),
         enabled,
         height,
         itemDistance: get(legend, 'itemDistance', legendDefaults.itemDistance),
@@ -59,6 +64,8 @@ export async function getPreparedLegend(args) {
         lineHeight,
         margin: get(legend, 'margin', legendDefaults.margin),
         type: legendType,
+        position: legendPosition,
+        panelWidth: legendPanelWidth,
         title: {
             enable: isTitleEnabled,
             text: titleText,
@@ -165,8 +172,69 @@ function getPagination(args) {
     });
     return { pages };
 }
+function getVerticalLegendItems(items, preparedLegend, panelWidth) {
+    const result = [];
+    const bodySelection = select(document.body);
+    items.forEach((item) => {
+        const itemSelection = preparedLegend.html
+            ? bodySelection
+                .append('div')
+                .html(item.name)
+                .style('position', 'absolute')
+                .style('display', 'inline-block')
+                .style('white-space', 'nowrap')
+            : bodySelection.append('text').text(item.name).style('white-space', 'nowrap');
+        itemSelection.style('font-size', preparedLegend.itemStyle.fontSize).each(function () {
+            const resultItem = clone(item);
+            const { height, width: textWidth } = this.getBoundingClientRect();
+            resultItem.height = height || preparedLegend.lineHeight;
+            const maxTextWidth = panelWidth - resultItem.symbol.width - resultItem.symbol.padding - 4;
+            if (textWidth > maxTextWidth) {
+                resultItem.overflowed = true;
+                resultItem.textWidth = maxTextWidth;
+            } else {
+                resultItem.textWidth = textWidth;
+            }
+            result.push([resultItem]);
+        }).remove();
+    });
+    return result;
+}
 export function getLegendComponents(args) {
     const { chartWidth, chartHeight, chartMargin, series, preparedLegend } = args;
+
+    // Right-panel legend: items stacked vertically on the right side
+    if (preparedLegend.position === 'right' && preparedLegend.enabled) {
+        const panelWidth = preparedLegend.panelWidth;
+        const flattenItems = getFlattenLegendItems(series, preparedLegend);
+        const items = getVerticalLegendItems(flattenItems, preparedLegend, panelWidth);
+        const maxLegendHeight = chartHeight - chartMargin.top - chartMargin.bottom;
+        let pagination;
+        if (preparedLegend.type === 'discrete') {
+            const totalHeight = items.reduce((acc, row) => {
+                return acc + (row.length ? Math.max(...row.map(({ height }) => height)) : 0);
+            }, 0);
+            preparedLegend.height = Math.min(totalHeight, maxLegendHeight);
+            if (totalHeight > maxLegendHeight) {
+                pagination = getPagination({
+                    items,
+                    maxLegendHeight: maxLegendHeight - preparedLegend.lineHeight,
+                    paginatorHeight: preparedLegend.lineHeight,
+                });
+            }
+        }
+        // Position: right side of chart, aligned to plot top
+        const offset = {
+            left: chartWidth - chartMargin.right - panelWidth + 52,
+            top: chartMargin.top,
+        };
+        return {
+            legendConfig: { offset, pagination, maxWidth: panelWidth, position: 'right' },
+            legendItems: items,
+        };
+    }
+
+    // Default bottom legend
     const maxLegendWidth = chartWidth - chartMargin.right - chartMargin.left;
     const maxLegendHeight = (chartHeight - chartMargin.top - chartMargin.bottom - preparedLegend.margin) / 2;
     const flattenLegendItems = getFlattenLegendItems(series, preparedLegend);
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/prepare-line.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/prepare-line.js
index 9902ea9..9504ba4 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/prepare-line.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/prepare-line.js
@@ -92,6 +92,10 @@ export function prepareLineSeries(args) {
             cursor: get(series, 'cursor', null),
             yAxis: get(series, 'yAxis', 0),
             tooltip: series.tooltip,
+            ...(typeof series.crisp === 'boolean' ? { crisp: series.crisp } : {}),
+            ...(typeof series.clip === 'boolean' ? { clip: series.clip } : {}),
+            ...(typeof series.zIndex === 'number' && Number.isFinite(series.zIndex) ? { zIndex: series.zIndex } : {}),
+            ...(series.states ? { states: series.states } : {}),
         };
         return prepared;
     }, []);
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/utils.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/utils.js
index e6a6af8..8259087 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/utils.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useSeries/utils.js
@@ -17,9 +17,11 @@ export function prepareLegendSymbol(series, symbolType) {
     var _a;
     const symbolOptions = ((_a = series.legend) === null || _a === void 0 ? void 0 : _a.symbol) || {};
     return {
-        shape: 'symbol',
-        symbolType: symbolType || SymbolType.Circle,
+        shape: symbolOptions.shape || 'symbol',
+        symbolType: symbolType || symbolOptions.symbolType || SymbolType.Circle,
         width: (symbolOptions === null || symbolOptions === void 0 ? void 0 : symbolOptions.width) || DEFAULT_LEGEND_SYMBOL_SIZE,
+        height: symbolOptions.height,
+        radius: symbolOptions.radius,
         padding: (symbolOptions === null || symbolOptions === void 0 ? void 0 : symbolOptions.padding) || DEFAULT_LEGEND_SYMBOL_PADDING,
     };
 }
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/line/index.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/line/index.js
index c58d313..85eabc9 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/line/index.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/line/index.js
@@ -26,17 +26,80 @@ export const LineSeriesShapes = (args) => {
             .y((d) => d.y);
         plotSvgElement.selectAll('*').remove();
         markersSvgElement.selectAll('*').remove();
+        const dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+        const pp = (v) => Math.round(v * dpr) / dpr;
+        const ps = 1 / dpr;
+        const hCrispBandYAlignPx = 0.35;
+        const isHorizontalCrisp = (seriesPrepared) => {
+            var _a;
+            const crisp = Boolean((_a = seriesPrepared === null || seriesPrepared === void 0 ? void 0 : seriesPrepared.series) === null || _a === void 0 ? void 0 : _a.crisp);
+            if (!crisp) {
+                return false;
+            }
+            const points = (seriesPrepared === null || seriesPrepared === void 0 ? void 0 : seriesPrepared.points) || [];
+            if (points.length < 2) {
+                return false;
+            }
+            const firstY = points[0].y;
+            return (typeof firstY === 'number' &&
+                Number.isFinite(firstY) &&
+                points.every((p) => typeof p.y === 'number' && Number.isFinite(p.y) && p.y === firstY));
+        };
+        const hBandData = preparedData.filter(isHorizontalCrisp);
+        const pathData = preparedData.filter((d) => !isHorizontalCrisp(d));
         const lineSelection = plotSvgElement
-            .selectAll('path')
-            .data(preparedData)
+            .selectAll(`path.${b('path-geom')}`)
+            .data(pathData, (d) => d.id)
             .join('path')
+            .attr('class', b('path-geom'))
+            .attr('data-series-line-id', (d) => String(d.id))
             .attr('d', (d) => line(d.points))
             .attr('fill', 'none')
             .attr('stroke', (d) => d.color)
             .attr('stroke-width', (d) => d.width)
-            .attr('stroke-linejoin', (d) => d.linecap)
-            .attr('stroke-linecap', (d) => d.linecap)
+            .attr('stroke-linejoin', 'miter')
+            .attr('stroke-linecap', 'butt')
             .attr('stroke-dasharray', (d) => getLineDashArray(d.dashStyle, d.width))
+            .attr('shape-rendering', null)
+            .attr('opacity', (d) => d.opacity)
+            .attr('cursor', (d) => d.series.cursor);
+        const crispBandRects = plotSvgElement
+            .selectAll(`rect.${b('y-grid-band')}`)
+            .data(hBandData, (d) => d.id)
+            .join('rect')
+            .attr('class', b('y-grid-band'))
+            .attr('data-series-line-id', (d) => String(d.id))
+            .attr('x', (d) => {
+            const xs = d.points.map((p) => p.x).filter((x) => typeof x === 'number' && Number.isFinite(x));
+            return xs.length ? Math.min(...xs) : 0;
+        })
+            .attr('width', (d) => {
+            const xs = d.points.map((p) => p.x).filter((x) => typeof x === 'number' && Number.isFinite(x));
+            if (!xs.length) {
+                return 0;
+            }
+            const w = Math.max(...xs) - Math.min(...xs);
+            return Number.isFinite(w) ? Math.max(0, w) : 0;
+        })
+            .attr('y', (d) => {
+            const ys = d.points
+                .map((p) => p.y)
+                .filter((y) => typeof y === 'number' && Number.isFinite(y));
+            const rawY = ys.length ? ys.reduce((a, b) => a + b, 0) / ys.length : d.points[0].y;
+            const lw = Number(d.width);
+            const linePx = Number.isFinite(lw) && lw > 0 ? lw : 1;
+            const h = Math.max(ps, linePx);
+            const top0 = pp(Math.round(rawY));
+            return top0 + ps / 2 - h / 2 + hCrispBandYAlignPx;
+        })
+            .attr('height', (d) => {
+            const lw = Number(d.width);
+            const linePx = Number.isFinite(lw) && lw > 0 ? lw : 1;
+            return Math.max(ps, linePx);
+        })
+            .attr('fill', (d) => d.color)
+            .style('stroke', 'none')
+            .attr('shape-rendering', 'crispEdges')
             .attr('opacity', (d) => d.opacity)
             .attr('cursor', (d) => d.series.cursor);
         let dataLabels = preparedData.reduce((acc, d) => {
@@ -70,19 +133,42 @@ export const LineSeriesShapes = (args) => {
             const selected = (data === null || data === void 0 ? void 0 : data.filter((d) => d.series.type === 'line')) || [];
             const selectedDataItems = selected.map((d) => d.data);
             const selectedSeriesIds = selected.map((d) => { var _a; return (_a = d.series) === null || _a === void 0 ? void 0 : _a.id; });
+            const applyHoverPaint = (elementSelection, dSelection) => {
+                var _a;
+                const initialColor = dSelection.color || '';
+                const nextColor = dSelection.hovered
+                    ? (((_a = color(initialColor)) === null || _a === void 0 ? void 0 : _a.brighter(hoverOptions === null || hoverOptions === void 0 ? void 0 : hoverOptions.brightness).toString()) || initialColor)
+                    : initialColor;
+                const n = elementSelection.node();
+                if (n && String(n.tagName).toLowerCase() === 'rect') {
+                    elementSelection.attr('fill', nextColor);
+                }
+                else {
+                    elementSelection.attr('stroke', nextColor);
+                }
+            };
             lineSelection.datum((d, index, list) => {
                 const elementSelection = select(list[index]);
                 const hovered = Boolean(hoverEnabled && selectedSeriesIds.includes(d.id));
                 if (d.hovered !== hovered) {
                     d.hovered = hovered;
-                    elementSelection.attr('stroke', (dSelection) => {
-                        var _a;
-                        const initialColor = dSelection.color || '';
-                        if (dSelection.hovered) {
-                            return (((_a = color(initialColor)) === null || _a === void 0 ? void 0 : _a.brighter(hoverOptions === null || hoverOptions === void 0 ? void 0 : hoverOptions.brightness).toString()) || initialColor);
-                        }
-                        return initialColor;
-                    });
+                    elementSelection.call((sel) => applyHoverPaint(sel, d));
+                }
+                return setActiveState({
+                    element: list[index],
+                    state: inactiveOptions,
+                    active: Boolean(!inactiveEnabled ||
+                        !selectedSeriesIds.length ||
+                        selectedSeriesIds.includes(d.id)),
+                    datum: d,
+                });
+            });
+            crispBandRects.datum((d, index, list) => {
+                const elementSelection = select(list[index]);
+                const hovered = Boolean(hoverEnabled && selectedSeriesIds.includes(d.id));
+                if (d.hovered !== hovered) {
+                    d.hovered = hovered;
+                    elementSelection.call((sel) => applyHoverPaint(sel, d));
                 }
                 return setActiveState({
                     element: list[index],
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/line/prepare-data.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/line/prepare-data.js
index 716b6bb..60ae53f 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/line/prepare-data.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/line/prepare-data.js
@@ -1,6 +1,15 @@
-import { getLabelsSize, getLeftPosition } from '../../../utils';
+import get from 'lodash/get';
+import { getDataCategoryValue, getLabelsSize, getLeftPosition } from '../../../utils';
 import { getFormattedValue } from '../../../utils/chart/format';
 import { getXValue, getYValue } from '../utils';
+function snapLinePathY(y) {
+    if (typeof y !== 'number' || !Number.isFinite(y)) {
+        return y;
+    }
+    var dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+    var rounded = Math.round(y);
+    return Math.round(rounded * dpr) / dpr;
+}
 async function getLabelData(point, series, xMax) {
     const text = getFormattedValue(Object.assign({ value: point.data.label || point.data.y }, series.dataLabels));
     const style = series.dataLabels.style;
@@ -60,9 +69,10 @@ export const prepareLineData = async (args) => {
                 yAxis: seriesYAxis,
                 yScale: seriesYScale,
             });
+            const x = getXValue({ point: d, points: s.data, xAxis, xScale });
             return {
-                x: getXValue({ point: d, points: s.data, xAxis, xScale }),
-                y: yValue === null ? null : yAxisTop + yValue,
+                x,
+                y: yValue === null ? null : snapLinePathY(yAxisTop + yValue),
                 active: true,
                 data: d,
                 series: s,
@@ -125,3 +135,4 @@ export const prepareLineData = async (args) => {
     }
     return acc;
 };
+
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/pie/index.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/pie/index.js
index 10f395f..4500a66 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/pie/index.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/pie/index.js
@@ -42,6 +42,11 @@ export function PieSeriesShapes(args) {
             return [];
         })
             .join('path')
+            .attr('transform', (d) => {
+            const dx = d.explodeDx || 0;
+            const dy = d.explodeDy || 0;
+            return dx || dy ? `translate(${dx}, ${dy})` : null;
+        })
             .attr('d', (d) => {
             const arcGenerator = arc()
                 .innerRadius(d.data.pie.innerRadius)
@@ -59,6 +64,11 @@ export function PieSeriesShapes(args) {
             .selectAll(segmentSelector)
             .data((pieData) => pieData.segments)
             .join('path')
+            .attr('transform', (d) => {
+            const dx = d.explodeDx || 0;
+            const dy = d.explodeDy || 0;
+            return dx || dy ? `translate(${dx}, ${dy})` : null;
+        })
             .attr('d', (d) => {
             const arcGenerator = arc()
                 .innerRadius(d.data.pie.innerRadius)
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/pie/prepare-data.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/pie/prepare-data.js
index d3f2ffa..f064dbb 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/pie/prepare-data.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/pie/prepare-data.js
@@ -78,7 +78,21 @@ export function preparePieData(args) {
             });
             return acc;
         }, []);
-        data.segments = pieGenerator(segments);
+        const explodeDirArc = arc().innerRadius(0).outerRadius(1).cornerRadius(0);
+        data.segments = pieGenerator(segments).map((seg) => {
+            var _a, _b;
+            const rawPoint = (_b = (_a = seg.data.series) === null || _a === void 0 ? void 0 : _a.data) !== null && _b !== void 0 ? _b : {};
+            const explodeOffset = typeof rawPoint.offset === 'number' ? rawPoint.offset : 0;
+            if (!explodeOffset) {
+                return seg;
+            }
+            const [vx, vy] = explodeDirArc.centroid(seg);
+            const len = Math.sqrt(vx * vx + vy * vy) || 1;
+            return Object.assign(Object.assign({}, seg), {
+                explodeDx: (vx / len) * explodeOffset,
+                explodeDy: (vy / len) * explodeOffset,
+            });
+        });
         return data;
     };
     const getLabels = async ({ series }) => {
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/utils.js b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/utils.js
index a689537..f9b9254 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/utils.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/hooks/useShapes/utils.js
@@ -1,6 +1,6 @@
 import { path, select } from 'd3';
 import get from 'lodash/get';
-import { getDataCategoryValue } from '../../utils';
+import { getDataCategoryValue, isBandScale } from '../../utils';
 const ONE_POINT_DOMAIN_DATA_CAPACITY = 3;
 export function getXValue(args) {
     const { point, points, xAxis, xScale } = args;
@@ -8,7 +8,23 @@ export function getXValue(args) {
         const xBandScale = xScale;
         const categories = get(xAxis, 'categories', []);
         const dataCategory = getDataCategoryValue({ axisDirection: 'x', categories, data: point });
-        return (xBandScale(dataCategory) || 0) + xBandScale.step() / 2;
+        const base = xBandScale(dataCategory);
+        if (base === undefined || base === null) {
+            return null;
+        }
+        if (!isBandScale(xBandScale)) {
+            return base;
+        }
+        const bw = xBandScale.bandwidth();
+        const domain = xBandScale.domain();
+        const catStr = String(dataCategory);
+        if (domain.length && String(domain[0]) === catStr) {
+            return base;
+        }
+        if (domain.length && String(domain[domain.length - 1]) === catStr) {
+            return base + bw;
+        }
+        return base + bw / 2;
     }
     let xLinearScale = xScale;
     const [xMinDomain, xMaxDomain] = xLinearScale.domain();
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/types/chart/legend.d.ts b/node_modules/@gravity-ui/charts/dist/cjs/types/chart/legend.d.ts
index 6b98a41..1612b3c 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/types/chart/legend.d.ts
+++ b/node_modules/@gravity-ui/charts/dist/cjs/types/chart/legend.d.ts
@@ -9,6 +9,14 @@ export interface ChartLegend {
      * @default 'discrete'
      */
     type?: 'discrete' | 'continuous';
+    /**
+     * The position of the legend relative to the chart area.
+     * - 'bottom' (default): horizontal legend below the chart
+     * - 'right': vertical panel on the right side of the chart
+     *
+     * @default bottom
+     */
+    position?: 'bottom' | 'right';
     /**
      * The horizontal alignment of the legend box within the chart area.
      *
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/axis-generators/bottom.js b/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/axis-generators/bottom.js
index 952800a..ce56c1a 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/axis-generators/bottom.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/axis-generators/bottom.js
@@ -1,8 +1,23 @@
 import { path, select } from 'd3';
-import { getAxisItems, getXAxisOffset, getXTickPosition } from '../axis';
+import { getAxisItems, getXAxisOffset, getXTickPosition, isBandScale } from '../axis';
 import { calculateCos, calculateSin } from '../math';
 import { getLabelsSize, setEllipsisForOverflowText } from '../text';
 const AXIS_BOTTOM_HTML_LABELS_DATA_ATTR = 'data-axis-bottom-html-labels';
+function isAxisBottomTickZero(d) {
+    if (d === 0 || d === '0') {
+        return true;
+    }
+    if (typeof d === 'number' && Number.isFinite(d) && !Number.isNaN(d) && Math.abs(d) < 1e-9) {
+        return true;
+    }
+    if (typeof d === 'string') {
+        const n = Number(d);
+        if (Number.isFinite(n) && Math.abs(n) < 1e-9) {
+            return true;
+        }
+    }
+    return false;
+}
 function addDomain(selection, options) {
     const { size, color } = options;
     const domainPath = selection
@@ -11,7 +26,7 @@ function addDomain(selection, options) {
         .enter()
         .insert('path', '.tick')
         .attr('class', 'domain')
-        .attr('d', `M0,0V0H${size}`);
+        .attr('d', `M0,0H${Math.round(size)}`);
     if (color) {
         domainPath.style('stroke', color);
     }
@@ -23,6 +38,7 @@ function appendSvgLabels(args) {
         .append('text')
         .html(ticks.labelFormat)
         .style('font-size', ((_a = ticks.labelsStyle) === null || _a === void 0 ? void 0 : _a.fontSize) || '')
+        .style('font-weight', (_a === null || _a === void 0 ? void 0 : _a.fontWeight) || '400')
         .attr('fill', 'currentColor')
         .attr('text-anchor', () => {
         if (ticks.rotation) {
@@ -45,7 +61,7 @@ function appendSvgLabels(args) {
         let elementX = 0;
         // add an ellipsis to the labels that go beyond the boundaries of the chart
         // and remove overlapping labels
-        labels
+        const sortedLabelNodes = labels
             .nodes()
             .map((element) => {
             const r = element.getBoundingClientRect();
@@ -57,9 +73,10 @@ function appendSvgLabels(args) {
         }, {})
             .sort((a, b) => {
             return a.left - b.left;
-        })
-            .forEach(function (item, i, nodes) {
-            var _a, _b, _c, _d;
+        });
+        const skipOverlapCulling = !ticks.rotation && sortedLabelNodes.length <= 120;
+        sortedLabelNodes.forEach(function (item, i, nodes) {
+            var _a, _b, _c;
             const { node, left, right: currentElementPositionRigth } = item;
             const currentElement = node;
             if (i === 0) {
@@ -78,23 +95,30 @@ function appendSvgLabels(args) {
             }
             else {
                 if (left < elementX) {
-                    (_c = currentElement.closest('.tick')) === null || _c === void 0 ? void 0 : _c.remove();
-                    return;
-                }
-                elementX = currentElementPositionRigth + ticks.labelsPaddings;
-                if (i === nodes.length - 1) {
-                    const prevElement = (_d = nodes[i - 1]) === null || _d === void 0 ? void 0 : _d.node;
-                    const text = select(currentElement);
-                    const prevElementPosition = prevElement === null || prevElement === void 0 ? void 0 : prevElement.getBoundingClientRect();
-                    const lackingSpace = Math.max(0, currentElementPositionRigth - right);
-                    if (lackingSpace) {
-                        const remainSpace = right - ((prevElementPosition === null || prevElementPosition === void 0 ? void 0 : prevElementPosition.right) || 0) - ticks.labelsPaddings;
-                        const translateX = -lackingSpace;
-                        text.style('transform', `translate(${translateX}px,${translateY}px)`);
-                        setEllipsisForOverflowText(text, remainSpace);
+                    if (!skipOverlapCulling) {
+                        (_c = currentElement.closest('.tick')) === null || _c === void 0 ? void 0 : _c.remove();
+                        return;
                     }
                 }
+                elementX = currentElementPositionRigth + ticks.labelsPaddings;
+            }
+        });
+    }
+    const zvn = ticks.zeroLabelVerticalNudgePx;
+    const zhn = ticks.zeroLabelHNudgePx;
+    const extraDy = typeof zvn === 'number' && Number.isFinite(zvn) ? zvn : 0;
+    const manualDx = typeof zhn === 'number' && Number.isFinite(zhn) ? zhn : 0;
+    const extraDx = manualDx;
+    if (extraDy !== 0 || extraDx !== 0) {
+        ticksSelection.each(function (d) {
+            if (!isAxisBottomTickZero(d)) {
+                return;
+            }
+            const text = select(this).select('text');
+            if (!text.node()) {
+                return;
             }
+            text.style('transform', `${transform} translate(${extraDx}px, ${extraDy}px)`);
         });
     }
 }
@@ -147,7 +171,7 @@ function appendHtmlLabels(args) {
     });
 }
 export async function axisBottom(args) {
-    const { boundsOffsetLeft = 0, boundsOffsetTop = 0, domain, htmlLayout, leftmostLimit = 0, scale, ticks: { count: ticksCount, items: tickItems, labelFormat = (value) => String(value), labelsHeight = 0, labelsHtml, labelsLineHeight, labelsMargin = 0, labelsMaxWidth = Infinity, labelsPaddings = 0, labelsStyle, maxTickCount, rotation = 0, tickColor, }, } = args;
+    const { boundsOffsetLeft = 0, boundsOffsetTop = 0, domain, htmlLayout, leftmostLimit = 0, scale, ticks: { count: ticksCount, items: tickItems, values: tickValues, labelFormat = (value) => String(value), labelsHeight = 0, labelsHtml, labelsLineHeight = 0, labelsMargin = 0, labelsMaxWidth = Infinity, labelsPaddings = 0, labelsStyle, maxTickCount, rotation = 0, tickColor, zeroLabelVerticalNudgePx: ticksZeroLabelVNudgePx, zeroLabelHNudgePx: ticksZeroLabelHNudgePx, }, } = args;
     const htmlSelection = select(htmlLayout);
     const offset = getXAxisOffset();
     const position = getXTickPosition({ scale, offset });
@@ -176,15 +200,39 @@ export async function axisBottom(args) {
         }
         const tickPath = path();
         tickItems === null || tickItems === void 0 ? void 0 : tickItems.forEach(([start, end]) => {
-            tickPath.moveTo(0, start);
-            tickPath.lineTo(0, end);
+            const nearAxis = Math.max(start, end);
+            const farAxis = Math.min(start, end);
+            const liftedStart = nearAxis === 0 ? -2 : nearAxis;
+            tickPath.moveTo(0, liftedStart);
+            tickPath.lineTo(0, farAxis);
         });
+        const domainBand = isBandScale(scale) ? scale.domain() : [];
+        const firstBandDomainVal = domainBand.length ? domainBand[0] : null;
+        const lastBandDomainVal = domainBand.length ? domainBand[domainBand.length - 1] : null;
+        const bandWidth = isBandScale(scale) ? scale.bandwidth() : 0;
+        const bandRange = isBandScale(scale) ? scale.range().map(Number) : [];
+        const xPlot0 = bandRange.length ? Math.min(...bandRange) : 0;
+        const xPlot1 = bandRange.length ? Math.max(...bandRange) : domain.size;
+        const singleBandDomain = Boolean(firstBandDomainVal != null &&
+            lastBandDomainVal != null &&
+            bandWidth > 0 &&
+            String(firstBandDomainVal) === String(lastBandDomainVal));
+        const xBandMid = Math.round((xPlot0 + xPlot1) / 2);
         const htmlLabelsData = labelsHtml
             ? values.map((v) => {
                 var _a;
+                const atBandRightEdge = !singleBandDomain &&
+                    lastBandDomainVal != null &&
+                    bandWidth > 0 &&
+                    String(v) === String(lastBandDomainVal);
+                const tickLeft = singleBandDomain
+                    ? xBandMid
+                    : atBandRightEdge
+                        ? xPlot1
+                        : position(v) + offset;
                 return {
                     content: String(v),
-                    left: position(v) + offset + boundsOffsetLeft,
+                    left: tickLeft + boundsOffsetLeft,
                     top: Math.abs(((_a = tickItems === null || tickItems === void 0 ? void 0 : tickItems[0]) === null || _a === void 0 ? void 0 : _a[1]) || 0) + labelsMargin + boundsOffsetTop,
                 };
             })
@@ -196,13 +244,28 @@ export async function axisBottom(args) {
             .join('g')
             .attr('class', 'tick')
             .attr('transform', function (d) {
-            const left = position(d) + offset;
-            return `translate(${left}, ${top})`;
+            const left = Math.round(position(d) + offset);
+            return `translate(${left}, ${Math.round(top)})`;
         });
         ticks
             .append('path')
             .attr('d', tickPath.toString())
-            .attr('stroke', tickColor !== null && tickColor !== void 0 ? tickColor : 'currentColor');
+            .attr('stroke', tickColor !== null && tickColor !== void 0 ? tickColor : 'currentColor')
+            .style('shape-rendering', 'crispEdges')
+            .style('stroke-width', '1');
+        if (singleBandDomain && firstBandDomainVal != null && bandWidth > 0) {
+            ticks
+                .filter((d) => String(d) === String(firstBandDomainVal))
+                .attr('transform', `translate(${xBandMid}, ${Math.round(top)})`);
+        }
+        if (lastBandDomainVal != null &&
+            bandWidth > 0 &&
+            String(lastBandDomainVal) !== String(firstBandDomainVal)) {
+            const lastLeft = Math.round(xPlot1);
+            ticks
+                .filter((d) => String(d) === String(lastBandDomainVal))
+                .attr('transform', `translate(${lastLeft}, ${Math.round(top)})`);
+        }
         // Remove tick that has the same x coordinate like domain
         selection
             .selectAll('.tick')
@@ -239,6 +302,8 @@ export async function axisBottom(args) {
                     labelsPaddings,
                     labelsStyle,
                     rotation,
+                    zeroLabelVerticalNudgePx: ticksZeroLabelVNudgePx,
+                    zeroLabelHNudgePx: ticksZeroLabelHNudgePx,
                 },
                 transform,
                 translateY,
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/axis.js b/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/axis.js
index 46ec3e5..47ca5e8 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/axis.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/axis.js
@@ -11,17 +11,21 @@ export function getTicksCount({ axis, range }) {
 export function isBandScale(scale) {
     return 'bandwidth' in scale && typeof scale.bandwidth === 'function';
 }
+export function isPointScale(scale) {
+    return Boolean(scale && typeof scale.step === 'function' && !isBandScale(scale));
+}
 export function getScaleTicks(scale, ticksCount) {
     if ('ticks' in scale && typeof scale.ticks === 'function') {
         return scale.ticks(ticksCount);
     }
-    if (isBandScale(scale)) {
+    if (isBandScale(scale) || isPointScale(scale)) {
         return scale.domain();
     }
     return [];
 }
 export function getXAxisOffset() {
-    return typeof window !== 'undefined' && window.devicePixelRatio > 1 ? 0 : 0.5;
+    // Fixed offset for consistent X geometry across monitor/laptop.
+    return 0;
 }
 function number(scale) {
     return (d) => Number(scale(d));
@@ -36,8 +40,79 @@ function center(scale, offset) {
 export function getXTickPosition({ scale, offset }) {
     return isBandScale(scale) ? center(scale, offset) : number(scale);
 }
+function isDenseConsecutiveIntegerDomain(domain) {
+    if (!domain || domain.length < 2) {
+        return false;
+    }
+    const nums = domain.map((d) => Number(d));
+    if (!nums.every((n) => Number.isFinite(n) && n === Math.trunc(n))) {
+        return false;
+    }
+    const min = Math.min(...nums);
+    const max = Math.max(...nums);
+    if (max - min + 1 !== nums.length) {
+        return false;
+    }
+    return new Set(nums).size === nums.length;
+}
+function niceStepInt(rough) {
+    if (!Number.isFinite(rough) || rough <= 0) {
+        return 1;
+    }
+    const exp = Math.floor(Math.log10(rough));
+    const pow = Math.pow(10, exp);
+    const frac = rough / pow;
+    let nf = 1;
+    if (frac <= 1)
+        nf = 1;
+    else if (frac <= 2)
+        nf = 2;
+    else if (frac <= 5)
+        nf = 5;
+    else
+        nf = 10;
+    return Math.max(1, Math.round(nf * pow));
+}
 export function getAxisItems({ scale, count, maxCount, }) {
     let values = getScaleTicks(scale, count);
+    if (isBandScale(scale) || isPointScale(scale)) {
+        const range = scale.range();
+        const span = Math.abs(Number(range[1]) - Number(range[0]));
+        const domain = scale.domain();
+        if (isDenseConsecutiveIntegerDomain(domain)) {
+            const nums = domain.map(Number);
+            const minV = Math.min(...nums);
+            const maxV = Math.max(...nums);
+            const spanV = maxV - minV;
+            const minPxPerTick = 32;
+            const maxLabelsByWidth = Math.max(4, Math.floor(span / minPxPerTick));
+            const hardCap = 50;
+            const bandLabelLimit = Math.min(maxLabelsByWidth, hardCap);
+            const targetTicks = Math.max(2, Math.min(bandLabelLimit, domain.length));
+            const rawStep = spanV / Math.max(1, targetTicks - 1);
+            const stepV = niceStepInt(rawStep);
+            const tickStrs = new Set();
+            tickStrs.add(String(maxV));
+            tickStrs.add(String(minV));
+            const start = Math.ceil(minV / stepV) * stepV;
+            for (let v = start; v <= maxV; v += stepV) {
+                tickStrs.add(String(v));
+            }
+            const domainIndex = new Map(domain.map((d, i) => [String(d), i]));
+            return Array.from(tickStrs)
+                .filter((s) => domainIndex.has(s))
+                .sort((a, b) => (domainIndex.get(a) ?? 0) - (domainIndex.get(b) ?? 0));
+        }
+        const minPxPerTick = 32;
+        const maxLabelsByWidth = Math.max(4, Math.floor(span / minPxPerTick));
+        const hardCap = 50;
+        const bandLabelLimit = Math.min(maxLabelsByWidth, hardCap);
+        if (values.length > bandLabelLimit) {
+            const step = Math.ceil(values.length / bandLabelLimit);
+            values = values.filter((_, i) => i % step === 0 || i === values.length - 1);
+        }
+        return values;
+    }
     if (maxCount && values.length > maxCount) {
         const step = Math.ceil(values.length / maxCount);
         values = values.filter((_, i) => i % step === 0);
@@ -137,3 +212,4 @@ export function getLabelFormatter({ axis, scale }) {
         });
     };
 }
+
diff --git a/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/index.js b/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/index.js
index b9c7bf0..997ab0e 100644
--- a/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/index.js
+++ b/node_modules/@gravity-ui/charts/dist/cjs/utils/chart/index.js
@@ -206,13 +206,10 @@ const extractCategoryValue = (args) => {
     if (typeof dataCategory === 'number') {
         categoryValue = categories[dataCategory];
     }
-    if (isNil(categoryValue)) {
-        throw new Error('It seems you are trying to get non-existing category value');
-    }
-    return categoryValue;
+    // Return null instead of throwing — caller must guard against null
+    return isNil(categoryValue) ? null : categoryValue;
 };
 export const getDataCategoryValue = (args) => {
     const { axisDirection, categories, data } = args;
-    const categoryValue = extractCategoryValue({ axisDirection, categories, data });
-    return categoryValue;
+    return extractCategoryValue({ axisDirection, categories, data });
 };
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/AxisX/AxisX.js b/node_modules/@gravity-ui/charts/dist/esm/components/AxisX/AxisX.js
index 3444e9d..cc5ea29 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/AxisX/AxisX.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/AxisX/AxisX.js
@@ -1,6 +1,6 @@
 import React from 'react';
 import { line, select } from 'd3';
-import { block, getAxisTitleRows, getBandsPosition, getLabelFormatter, getLineDashArray, getMaxTickCount, getTicksCount, handleOverflowingText, } from '../../utils';
+import { block, getAxisTitleRows, getBandsPosition, getLabelFormatter, getLineDashArray, getMaxTickCount, getTicksCount, handleOverflowingText, isBandScale, } from '../../utils';
 import { axisBottom } from '../../utils/chart/axis-generators';
 import './styles.css';
 const b = block('axis');
@@ -28,15 +28,21 @@ export function getTitlePosition(args) {
     return { x, y };
 }
 export const AxisX = React.memo(function AxisX(props) {
-    const { axis, boundsOffsetLeft, boundsOffsetTop, height: totalHeight, htmlLayout, leftmostLimit, plotAfterRef, plotBeforeRef, split, scale, width, } = props;
+    const { axis, boundsOffsetLeft, boundsOffsetTop, height: totalHeight, htmlLayout, leftmostLimit, plotAfterRef, plotBeforeRef, split, scale, width, xAxisBaselineY, } = props;
+    /** Y in plot coords where the X baseline sits (matches shapes/grid bottom); defaults to bounds height. */
+    const plotBottomY = typeof xAxisBaselineY === 'number' && Number.isFinite(xAxisBaselineY) ? xAxisBaselineY : totalHeight;
     const ref = React.useRef(null);
     React.useEffect(() => {
+        let cancelled = false;
         (async () => {
             if (!ref.current || !htmlLayout) {
                 return;
             }
             const svgElement = select(ref.current);
             svgElement.selectAll('*').remove();
+            if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
+                select(plotAfterRef.current).selectAll('[data-gcharts-x-corner-tick]').remove();
+            }
             if (!axis.visible) {
                 return;
             }
@@ -54,7 +60,7 @@ export const AxisX = React.memo(function AxisX(props) {
                 boundsOffsetLeft,
                 boundsOffsetTop,
                 domain: {
-                    size: width,
+                    size: width + 7,
                     color: axis.lineColor,
                 },
                 htmlLayout,
@@ -73,12 +79,126 @@ export const AxisX = React.memo(function AxisX(props) {
                     labelsStyle: axis.labels.style,
                     maxTickCount: getMaxTickCount({ axis, width }),
                     rotation: axis.labels.rotation,
+                    zeroLabelVerticalNudgePx: axis.zeroLabelVerticalNudgePx,
+                    zeroLabelHNudgePx: axis.zeroLabelHNudgePx,
                 },
             });
+            if (cancelled || !ref.current) {
+                return;
+            }
             svgElement.call(xAxisGenerator).attr('class', b());
+            // Use <rect fill> instead of <path stroke> for all axis lines.
+            // A stroked path at integer coordinates straddles pixel boundaries and renders as
+            // 2 physical pixels at DPR≠1 (e.g. Windows 125% scaling = DPR 1.25).
+            // A filled <rect> at physical-pixel-aligned coordinates is guaranteed 1 physical pixel.
+            const _dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+            // _pp(v): floor v to the nearest physical pixel boundary (in CSS px units).
+            const _pp = (v) => Math.round(v * _dpr) / _dpr;
+            const _ps = 1 / _dpr; // 1 physical pixel in CSS px units
+            const outerTickLength = typeof axis.tickLength === 'number' && Number.isFinite(axis.tickLength)
+                ? axis.tickLength
+                : 6;
+            const rightTickH = typeof axis.xTickLengthAtRightPx === 'number' &&
+                Number.isFinite(axis.xTickLengthAtRightPx) &&
+                axis.xTickLengthAtRightPx > 0
+                ? axis.xTickLengthAtRightPx
+                : outerTickLength;
+            const xAxisDomainExtendRight = typeof axis.xDomainExtendRightPx === 'number' &&
+                Number.isFinite(axis.xDomainExtendRightPx) &&
+                axis.xDomainExtendRightPx >= 0
+                ? axis.xDomainExtendRightPx
+                : 0;
+            // Remove D3 domain path; replace with a physical-pixel-perfect rect.
+            svgElement.selectAll('.domain').remove();
+            svgElement.selectAll('.gcharts-axis__domain-rect').remove();
+            // Use .style() so inline styles override any CSS class rules (fill/stroke from library CSS).
+            svgElement.append('rect')
+                .attr('class', 'gcharts-axis__domain-rect')
+                .attr('x', 0)
+                .attr('y', _pp(0))
+                .attr('width', Math.round(width) + xAxisDomainExtendRight)
+                .attr('height', _ps)
+                .attr('shape-rendering', 'crispEdges')
+                .style('fill', '#000000')
+                .style('stroke', 'none');
+            // Remove all D3 tick marks and library duplicates; replace with per-tick rects.
+            svgElement.selectAll('.tick line').remove();
+            svgElement.selectAll('.gcharts-axis__outer-tick-batch').remove();
+            svgElement.selectAll('.gcharts-axis__tick-rect').remove();
+            const outerTickXSeen = new Set();
+            const xBand = isBandScale(axisScale);
+            /** Right-edge stub (e.g. x=0 reverse): paint in plotAfter so Y-domain rect does not cover it. */
+            let cornerTickPlot = null;
+            svgElement.selectAll('.tick').each(function () {
+                const tick = select(this);
+                tick.selectAll('.gcharts-axis__outer-tick').remove();
+                tick.selectAll('path').remove();
+                const currentTransform = tick.attr('transform') || '';
+                const tMatch = currentTransform.match(/translate\(\s*([^,)\s]+)/);
+                if (tMatch) {
+                    const rawX = parseFloat(tMatch[1]);
+                    if (xBand) {
+                        tick.attr('transform', `translate(${_pp(rawX)}, 0)`);
+                        if (rawX > width + 2) {
+                            return;
+                        }
+                        svgElement.append('rect')
+                            .attr('class', 'gcharts-axis__tick-rect')
+                            .attr('x', _pp(rawX))
+                            .attr('y', 0)
+                            .attr('width', _ps)
+                            .attr('height', outerTickLength)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', '#000000')
+                            .style('stroke', 'none');
+                        return;
+                    }
+                    const tx = Math.round(rawX);
+                    tick.attr('transform', 'translate(' + tx + ', 0)');
+                    if (outerTickXSeen.has(tx)) {
+                        return;
+                    }
+                    outerTickXSeen.add(tx);
+                    if (tx > Math.round(width)) {
+                        return;
+                    }
+                    const axisRightX = Math.round(width);
+                    const atRightEdge = tx >= axisRightX - 1;
+                    const tickX = atRightEdge
+                        ? Math.max(0, _pp(axisRightX))
+                        : Math.min(_pp(tx), _pp(width) - _ps);
+                    const tickHeight = atRightEdge ? Math.max(rightTickH, _ps) : outerTickLength;
+                    if (atRightEdge) {
+                        cornerTickPlot = { x: Math.max(0, tickX), h: tickHeight };
+                    }
+                    else {
+                        svgElement.append('rect')
+                            .attr('class', 'gcharts-axis__tick-rect')
+                            .attr('x', Math.max(0, tickX))
+                            .attr('y', 0)
+                            .attr('width', _ps)
+                            .attr('height', tickHeight)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', '#000000')
+                            .style('stroke', 'none');
+                    }
+                }
+            });
+            if (!xBand && axis.omitCornerVerticalTick) {
+                const axisRightX = Math.round(width);
+                if (!outerTickXSeen.has(axisRightX) && !cornerTickPlot) {
+                    cornerTickPlot = {
+                        x: Math.max(0, _pp(axisRightX)),
+                        h: Math.max(rightTickH, _ps),
+                    };
+                }
+            }
             // add an axis header if necessary
             if (axis.title.text) {
                 const titleRows = await getAxisTitleRows({ axis, textMaxWidth: width });
+                if (cancelled || !ref.current) {
+                    return;
+                }
                 const titleClassName = b('title');
                 svgElement.selectAll(`.${titleClassName}`).remove();
                 svgElement
@@ -89,6 +209,9 @@ export const AxisX = React.memo(function AxisX(props) {
                     return `translate(${x}, ${y})`;
                 })
                     .attr('font-size', axis.title.style.fontSize)
+                    .attr('font-weight', axis.title.style.fontWeight || '500')
+                    .attr('fill', '#000000')
+                    .attr('text-rendering', 'geometricPrecision')
                     .attr('text-anchor', 'middle')
                     .selectAll('tspan')
                     .data(titleRows)
@@ -184,7 +307,7 @@ export const AxisX = React.memo(function AxisX(props) {
                         const plotLineValue = Number(axisScale(plotLine.value));
                         const points = [
                             [plotLineValue, 0],
-                            [plotLineValue, totalHeight],
+                            [plotLineValue, plotBottomY],
                         ];
                         itemSelection
                             .append('path')
@@ -209,7 +332,51 @@ export const AxisX = React.memo(function AxisX(props) {
                 setPlotLines(plotBeforeContainer, axis.plotLines.filter((d) => d.layerPlacement === 'before'));
                 setPlotLines(plotAfterContainer, axis.plotLines.filter((d) => d.layerPlacement === 'after'));
             }
+            // After plotAfter cleanup + bands/lines so the corner tick is not covered or removed by later sync work.
+            if (cornerTickPlot) {
+                const corner = cornerTickPlot;
+                const paintCornerTick = () => {
+                    if (cancelled) {
+                        return;
+                    }
+                    if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
+                        const pa = select(plotAfterRef.current);
+                        pa.selectAll('[data-gcharts-x-corner-tick]').remove();
+                        pa.append('rect')
+                            .attr('data-gcharts-x-corner-tick', '1')
+                            .attr('class', 'gcharts-axis__tick-rect')
+                            .attr('x', corner.x)
+                            .attr('y', plotBottomY)
+                            .attr('width', _ps)
+                            .attr('height', corner.h)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', '#000000')
+                            .style('stroke', 'none');
+                    }
+                    else {
+                        svgElement.append('rect')
+                            .attr('class', 'gcharts-axis__tick-rect')
+                            .attr('x', corner.x)
+                            .attr('y', 0)
+                            .attr('width', _ps)
+                            .attr('height', corner.h)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', '#000000')
+                            .style('stroke', 'none');
+                    }
+                };
+                queueMicrotask(paintCornerTick);
+            }
         })();
+        return () => {
+            cancelled = true;
+            if (ref.current) {
+                select(ref.current).selectAll('*').remove();
+            }
+            if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
+                select(plotAfterRef.current).selectAll('[data-gcharts-x-corner-tick]').remove();
+            }
+        };
     }, [
         axis,
         boundsOffsetLeft,
@@ -222,6 +389,7 @@ export const AxisX = React.memo(function AxisX(props) {
         split,
         totalHeight,
         width,
+        xAxisBaselineY,
     ]);
     return React.createElement("g", { ref: ref });
 });
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/AxisX/styles.css b/node_modules/@gravity-ui/charts/dist/esm/components/AxisX/styles.css
index 5685097..7c6bac4 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/AxisX/styles.css
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/AxisX/styles.css
@@ -1,16 +1,30 @@
 .gcharts-axis .domain {
-  stroke: var(--g-color-line-generic-active);
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
 }
 .gcharts-axis .tick text {
-  color: var(--g-color-text-secondary);
+  color: #000000;
+  fill: #000000;
   dominant-baseline: text-after-edge;
+  text-rendering: optimizeSpeed;
+  font-weight: 400;
 }
-.gcharts-axis .tick line, .gcharts-axis .tick path {
-  stroke: var(--g-color-line-generic);
+.gcharts-axis .gcharts-axis__outer-tick {
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
+}
+.gcharts-axis .gcharts-axis__outer-tick-batch {
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
 }
 .gcharts-axis__title {
   dominant-baseline: text-after-edge;
-  fill: var(--g-color-text-secondary);
+  fill: #000000;
+  text-rendering: optimizeSpeed;
+  font-weight: 400;
 }
 .gcharts-axis__title tspan {
   dominant-baseline: text-after-edge;
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/AxisY.js b/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/AxisY.js
index c0e99ce..1b235b9 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/AxisY.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/AxisY.js
@@ -25,30 +25,91 @@ export const AxisY = (props) => {
         if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
             plotAfterContainer = select(plotAfterRef.current);
             plotAfterContainer.selectAll(`[${plotDataAttr}]`).remove();
+            plotAfterContainer.selectAll('[data-y-outer-ticks]').remove();
+            plotAfterContainer.selectAll('[data-y-domain-overlay]').remove();
         }
         if (preparedAxisData.title) {
-            svgElement
+            const tt = preparedAxisData.title;
+            const titleTextSelection = svgElement
                 .append('g')
                 .attr('class', b('title'))
+                .attr('transform', `translate(${Math.round(tt.x)}, ${Math.round(tt.y)}) rotate(${tt.rotate}) translate(0, ${Math.round(tt.offset)})`)
                 .append('text')
                 .attr('text-anchor', 'start')
-                .style('dominant-baseline', 'text-after-edge')
-                .style('transform', `translate(${preparedAxisData.title.x}px, ${preparedAxisData.title.y}px) rotate(${preparedAxisData.title.rotate}deg) translate(0px, ${preparedAxisData.title.offset}px)`)
-                .attr('font-size', preparedAxisData.title.style.fontSize)
-                .selectAll('tspan')
-                .data(preparedAxisData.title.content)
-                .join('tspan')
-                .html((d) => d.text)
-                .attr('x', (d) => d.x)
-                .attr('y', (d) => d.y)
-                .attr('text-anchor', 'start');
+                .style('dominant-baseline', 'central')
+                .attr('text-rendering', 'optimizeLegibility')
+                .attr('font-size', tt.style.fontSize)
+                .attr('font-weight', tt.style.fontWeight || '500')
+                .attr('fill', '#000000')
+                .style('letter-spacing', (tt.style && tt.style.letterSpacing) || 'normal');
+            if (tt.content.length === 1) {
+                const row = tt.content[0];
+                titleTextSelection
+                    .text(row.text)
+                    .attr('x', row.x)
+                    .attr('y', row.y);
+            }
+            else {
+                titleTextSelection
+                    .selectAll('tspan')
+                    .data(tt.content)
+                    .join('tspan')
+                    .text((d) => d.text)
+                    .attr('x', (d) => (d.x != null ? d.x : null))
+                    .attr('y', (d) => (d.y != null ? d.y : null))
+                    .attr('dx', (d) => (d.dx != null ? d.dx : null))
+                    .attr('dy', (d) => (d.dy != null ? d.dy : null))
+                    .attr('text-anchor', 'start');
+            }
+        }
+        // Compute tick Y extents first so the domain line aligns exactly with actual tick positions.
+        let bottomMostTickY = -Infinity;
+        let topMostTickY = Infinity;
+        for (const t of preparedAxisData.ticks) {
+            const ty = t.line ? t.line.points[0][1] : (t.svgLabel ? t.svgLabel.y : null);
+            if (ty !== null) {
+                if (ty > bottomMostTickY) {
+                    bottomMostTickY = ty;
+                }
+                if (ty < topMostTickY) {
+                    topMostTickY = ty;
+                }
+            }
         }
+        const domainX = preparedAxisData.domain
+            ? Math.round(preparedAxisData.domain.start[0])
+            : null;
+        // Use <rect fill> instead of <path stroke> for all axis lines.
+        // A stroked path at integer coordinates straddles pixel boundaries and renders as
+        // 2 physical pixels at DPR≠1 (e.g. Windows 125% scaling = DPR 1.25).
+        // A filled <rect> at physical-pixel-aligned coordinates is guaranteed 1 physical pixel.
+        const _dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+        const _pp = (v) => Math.round(v * _dpr) / _dpr;
+        const _ps = 1 / _dpr; // 1 physical pixel in CSS px units
         if (preparedAxisData.domain) {
-            svgElement
-                .append('path')
-                .attr('class', b('domain'))
-                .attr('d', lineGenerator([preparedAxisData.domain.start, preparedAxisData.domain.end]))
-                .style('stroke', preparedAxisData.domain.lineColor);
+            // dStartY = bottom end of Y-axis; no extra "foot" below y=0,
+            // чтобы в углу (0,0) не рисовался второй вертикальный штрих.
+            // dEndY   = top end of Y-axis (smallest SVG Y = highest on screen) = exactly at topmost tick
+            const dStartY = bottomMostTickY > -Infinity
+                ? _pp(Math.round(bottomMostTickY))
+                : _pp(Math.round(preparedAxisData.domain.end[1]));
+            const dEndY = topMostTickY < Infinity
+                ? _pp(Math.round(topMostTickY))
+                : _pp(Math.round(preparedAxisData.domain.start[1]));
+            const domainContainer = plotAfterContainer || svgElement;
+            // Physical-pixel-wide vertical rect for the Y-axis domain line.
+            // Use .style() (inline style) not .attr() so CSS class rules cannot override fill/stroke.
+            domainContainer
+                .append('rect')
+                .attr('class', 'gcharts-y-axis__domain-rect')
+                .attr('data-y-domain-overlay', 1)
+                .attr('x', _pp(domainX))
+                .attr('y', dEndY)
+                .attr('width', _ps)
+                .attr('height', dStartY - dEndY)
+                .attr('shape-rendering', 'crispEdges')
+                .style('fill', '#000000')
+                .style('stroke', 'none');
         }
         const tickClassName = b('tick');
         const ticks = svgElement
@@ -58,12 +119,61 @@ export const AxisY = (props) => {
             .join('g')
             .attr('class', tickClassName);
         const labelClassName = b('label');
+        const tickLength = typeof preparedAxisData.tickLength === 'number' &&
+            Number.isFinite(preparedAxisData.tickLength) &&
+            preparedAxisData.tickLength > 0
+            ? preparedAxisData.tickLength
+            : 8;
+        const bottomTickLength = typeof preparedAxisData.yTickLengthAtBottomPx === 'number' &&
+            Number.isFinite(preparedAxisData.yTickLengthAtBottomPx) &&
+            preparedAxisData.yTickLengthAtBottomPx > 0
+            ? preparedAxisData.yTickLengthAtBottomPx
+            : tickLength;
+        const outerTickSegments = [];
+        const outerTickYSeen = new Set();
         ticks.each(function () {
             var _a;
             const tickSelection = select(this);
             const tickData = tickSelection.datum();
-            if (tickData.line) {
-                tickSelection.append('path').attr('d', lineGenerator(tickData.line.points));
+            const rawTickY = tickData.line
+                ? tickData.line.points[0][1]
+                : (tickData.svgLabel ? tickData.svgLabel.y : 0);
+            const roundedTickY = Math.round(rawTickY);
+            const isBottomTick = bottomMostTickY > -Infinity && Math.abs(rawTickY - bottomMostTickY) < 1;
+            if (tickData.line && !isBottomTick) {
+                const xLeft = tickData.line.points[0][0];
+                const xRight = tickData.line.points[1][0];
+                let gridX0 = xLeft;
+                let gridX1 = xRight;
+                if (domainX !== null) {
+                    if (xRight <= domainX) {
+                        gridX1 = xRight;
+                    } else {
+                        gridX0 = Math.max(xLeft, domainX + tickLength);
+                    }
+                }
+                if (gridX1 > gridX0) {
+                    // Physical-pixel-tall horizontal rect for each grid line.
+                    tickSelection.append('rect')
+                        .attr('class', 'gcharts-y-axis__grid-seg-rect')
+                        .attr('x', gridX0)
+                        .attr('y', _pp(roundedTickY))
+                        .attr('width', gridX1 - gridX0)
+                        .attr('height', _ps)
+                        .attr('shape-rendering', 'crispEdges')
+                        .style('fill', 'var(--g-color-line-generic)')
+                        .style('stroke', 'none');
+                }
+            }
+            if (domainX !== null) {
+                // Show outer tick mark for ALL Y-axis values including the bottommost (y=0).
+                // The grid line at y=0 is still skipped (it would overlap the X-axis domain),
+                // but the horizontal dash on the Y-axis itself must be visible at every value.
+                const endX = domainX + (isBottomTick ? bottomTickLength : tickLength);
+                if (!outerTickYSeen.has(roundedTickY)) {
+                    outerTickYSeen.add(roundedTickY);
+                    outerTickSegments.push({ tickStartX: domainX, endX, roundedTickY });
+                }
             }
             if (tickData.svgLabel) {
                 const label = tickData.svgLabel;
@@ -89,6 +199,7 @@ export const AxisY = (props) => {
                     .attr('class', labelClassName)
                     .style('dominant-baseline', 'text-before-edge')
                     .style('font-size', label.style.fontSize)
+                    .style('font-weight', label.style.fontWeight || '400')
                     .style('fill', (_a = label.style.fontColor) !== null && _a !== void 0 ? _a : '');
             }
         });
@@ -147,13 +258,39 @@ export const AxisY = (props) => {
                     .attr(plotDataAttr, 1)
                     .attr(plotLineDataAttr, 1)
                     .style('transform', (d) => `translate(${d.x}px, ${d.y}px)`);
-                plotLinesSelection
-                    .append('path')
-                    .attr('d', (d) => lineGenerator(d.points))
-                    .attr('stroke', (d) => d.color)
-                    .attr('stroke-width', (d) => d.lineWidth)
-                    .attr('stroke-dasharray', (d) => getLineDashArray(d.dashStyle, d.lineWidth))
-                    .attr('opacity', (d) => d.opacity);
+                plotLinesSelection.each(function () {
+                    const itemSelection = select(this);
+                    const plotLine = itemSelection.datum();
+                    const points = plotLine.points || [];
+                    const isHorizontal = points.length >= 2 && points.every((p) => p[1] === points[0][1]);
+                    if (isHorizontal) {
+                        const rawY = points[0][1];
+                        const x0 = Math.min(points[0][0], points[points.length - 1][0]);
+                        const x1 = Math.max(points[0][0], points[points.length - 1][0]);
+                        itemSelection
+                            .append('rect')
+                            .attr('x', x0)
+                            .attr('y', _pp(rawY))
+                            .attr('width', Math.max(0, x1 - x0))
+                            .attr('height', _ps)
+                            .attr('shape-rendering', 'crispEdges')
+                            .style('fill', plotLine.color)
+                            .style('stroke', 'none')
+                            .style('opacity', plotLine.opacity);
+                    }
+                    else {
+                        itemSelection
+                            .append('path')
+                            .attr('d', lineGenerator(plotLine.points))
+                            .attr('stroke', plotLine.color)
+                            .attr('stroke-width', plotLine.lineWidth)
+                            .attr('stroke-dasharray', getLineDashArray(plotLine.dashStyle, plotLine.lineWidth))
+                            .style('stroke-linecap', 'butt')
+                            .style('shape-rendering', 'geometricPrecision')
+                            .attr('fill', 'none')
+                            .attr('opacity', plotLine.opacity);
+                    }
+                });
                 plotLinesSelection.each(function () {
                     var _a, _b;
                     const itemSelection = select(this);
@@ -175,8 +312,58 @@ export const AxisY = (props) => {
             setPlotLines(plotBeforeContainer, preparedAxisData.plotLines.filter((item) => item.layerPlacement === 'before'));
             setPlotLines(plotAfterContainer, preparedAxisData.plotLines.filter((item) => item.layerPlacement === 'after'));
         }
+        if (outerTickSegments.length) {
+            // Physical-pixel-tall horizontal rects for Y-axis outer tick marks.
+            const appendOuterTicks = (container) => {
+                if (!container) {
+                    return;
+                }
+                const layer = container
+                    .append('g')
+                    .attr('data-y-outer-ticks', preparedAxisData.id)
+                    .attr('class', 'gcharts-y-axis__outer-ticks-overlay');
+                outerTickSegments.forEach((seg) => {
+                    layer.append('rect')
+                        .attr('class', 'gcharts-y-axis__outer-tick-rect')
+                        .attr('x', seg.tickStartX)
+                        .attr('y', _pp(seg.roundedTickY))
+                        .attr('width', seg.endX - seg.tickStartX)
+                        .attr('height', _ps)
+                        .attr('shape-rendering', 'crispEdges')
+                        .style('fill', '#000000')
+                        .style('stroke', 'none');
+                });
+            };
+            if (plotAfterContainer) {
+                appendOuterTicks(plotAfterContainer);
+            }
+            else {
+                outerTickSegments.forEach((seg) => {
+                    svgElement.append('rect')
+                        .attr('class', 'gcharts-y-axis__outer-tick-rect')
+                        .attr('x', seg.tickStartX)
+                        .attr('y', _pp(seg.roundedTickY))
+                        .attr('width', seg.endX - seg.tickStartX)
+                        .attr('height', _ps)
+                        .attr('shape-rendering', 'crispEdges')
+                        .style('fill', '#000000')
+                        .style('stroke', 'none');
+                });
+            }
+        }
+        return () => {
+            if (ref.current) {
+                select(ref.current).selectAll('*').remove();
+            }
+            if (plotAfterRef === null || plotAfterRef === void 0 ? void 0 : plotAfterRef.current) {
+                const after = select(plotAfterRef.current);
+                after.selectAll('[data-y-outer-ticks]').remove();
+                after.selectAll('[data-y-domain-overlay]').remove();
+            }
+        };
     }, [lineGenerator, plotAfterRef, plotBeforeRef, preparedAxisData]);
     return (React.createElement(React.Fragment, null,
         React.createElement(HtmlLayer, { preparedData: { htmlElements: htmlLabels }, htmlLayout: htmlLayout }),
         React.createElement("g", { ref: ref, className: b() })));
 };
+
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/prepare-axis-data.js b/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/prepare-axis-data.js
index 052af4f..ee6cc06 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/prepare-axis-data.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/prepare-axis-data.js
@@ -95,7 +95,7 @@ async function getSvgAxisLabel({ getTextSize, text, axis, top, left, labelMaxHei
         });
     }
     const x = axis.position === 'left' ? left - axis.labels.margin : left + axis.labels.margin;
-    const y = top;
+    const y = top + (typeof axis.labels.y === 'number' ? axis.labels.y : 0);
     const svgLabel = {
         title: content.length > 1 || ((_a = content[0]) === null || _a === void 0 ? void 0 : _a.text) !== text ? text : undefined,
         content: content,
@@ -117,7 +117,7 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
     if (axis.visible) {
         domain = {
             start: [domainX, axisPlotTopPosition],
-            end: [domainX, axisPlotTopPosition + axisHeight],
+            end: [domainX, axisPlotTopPosition + axisHeight + 2],
             lineColor: (_c = axis.lineColor) !== null && _c !== void 0 ? _c : '',
         };
     }
@@ -127,9 +127,15 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
     const values = getTickValues({ scale, axis, labelLineHeight, series });
     const tickStep = getMinSpaceBetween(values, (d) => Number(d.value));
     const labelMaxHeight = values.length > 1 ? values[0].y - values[1].y - axis.labels.padding * 2 : axisHeight;
+    // Same snap as AxisY grid rects (_pp ∘ Math.round) so labels + tick paths match physical pixels.
+    const dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+    const snapTickY = (yFloat) => {
+        const r = Math.round(yFloat);
+        return Math.round(r * dpr) / dpr;
+    };
     for (let i = 0; i < values.length; i++) {
         const tickValue = values[i];
-        const y = axisPlotTopPosition + tickValue.y;
+        const y = snapTickY(axisPlotTopPosition + tickValue.y);
         let svgLabel = null;
         let htmlLabel = null;
         if (axis.labels.enabled) {
@@ -149,7 +155,7 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
                 htmlLabel = {
                     content,
                     x,
-                    y: top - size.height / 2,
+                    y: top - size.height / 2 + (typeof axis.labels.y === 'number' ? axis.labels.y : 0),
                     size,
                     style: axis.labels.style,
                 };
@@ -181,6 +187,25 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
             htmlLabel,
         });
     }
+    {
+        const seenRoundY = new Set();
+        const uniqueTicks = [];
+        for (const t of ticks) {
+            const rawY = t.line ? t.line.points[0][1] : (t.svgLabel ? t.svgLabel.y : undefined);
+            if (rawY === undefined) {
+                uniqueTicks.push(t);
+                continue;
+            }
+            const k = Math.round(rawY);
+            if (seenRoundY.has(k)) {
+                continue;
+            }
+            seenRoundY.add(k);
+            uniqueTicks.push(t);
+        }
+        ticks.length = 0;
+        ticks.push(...uniqueTicks);
+    }
     let labelsWidth = ticks.reduce((acc, item) => { var _a, _b, _c, _d; return Math.max(acc, (_d = (_b = (_a = item.svgLabel) === null || _a === void 0 ? void 0 : _a.size.width) !== null && _b !== void 0 ? _b : (_c = item.htmlLabel) === null || _c === void 0 ? void 0 : _c.size.width) !== null && _d !== void 0 ? _d : 0); }, 0);
     labelsWidth = Math.min(axis.labels.width, labelsWidth);
     let title = null;
@@ -191,6 +216,8 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
         const cos = Math.abs(calculateCos(rotateAngle));
         const titleContent = [];
         const titleMaxWidth = sin * axisHeight;
+        let passengerInlineTspans = false;
+        let passengerTitleBox = null;
         if (axis.title.maxRowCount > 1) {
             const titleTextRows = await wrapText({
                 text: axis.title.text,
@@ -210,6 +237,54 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
                 });
             }
         }
+        else if (axis.title.text.replace(/\s+/g, ' ').trim() === 'Число пассажиров') {
+            const fsStr = String((axis.title.style && axis.title.style.fontSize) || '13px');
+            const fontSizePx = parseFloat(fsStr.replace(/^([\d.]+).*$/, '$1')) || 13;
+            const widenCheIPx = fontSizePx * 0.03;
+            const trackISCpx = fontSizePx * 0.026;
+            const bumpLoPx = 0;
+            const pullOxPx = -fontSizePx * 0.024;
+            const trackPassePx = fontSizePx * 0.021;
+            const bumpZhePx = fontSizePx * 0.036;
+            const trackIroPx = fontSizePx * 0.006;
+            const trackRoInZhirovPx = -fontSizePx * 0.01;
+            const tightPassejPx = -fontSizePx * 0.006;
+            const parts = [
+                { text: 'Ч', dx: 0 },
+                { text: 'и', dx: widenCheIPx },
+                { text: 'с', dx: trackISCpx },
+                { text: 'л', dx: bumpLoPx },
+                { text: 'о', dx: pullOxPx },
+                { text: ' ', dx: 0 },
+                { text: 'п', dx: 0 },
+                { text: 'а', dx: trackPassePx },
+                { text: 'с', dx: trackPassePx },
+                { text: 'с', dx: trackPassePx },
+                { text: 'а', dx: trackPassePx },
+                { text: 'ж', dx: bumpZhePx },
+                { text: 'и', dx: tightPassejPx },
+                { text: 'р', dx: trackIroPx },
+                { text: 'о', dx: trackRoInZhirovPx },
+                { text: 'в', dx: trackIroPx },
+            ];
+            let totalW = 0;
+            let maxH = 0;
+            for (let pi = 0; pi < parts.length; pi++) {
+                const p = parts[pi];
+                const sz = await getTitleTextSize(p.text);
+                totalW += p.dx + sz.width;
+                maxH = Math.max(maxH, sz.height);
+                titleContent.push({
+                    text: p.text,
+                    x: pi === 0 ? 0 : undefined,
+                    y: pi === 0 ? 0 : undefined,
+                    dx: typeof p.dx === 'number' && p.dx !== 0 ? p.dx : undefined,
+                    size: sz,
+                });
+            }
+            passengerInlineTspans = true;
+            passengerTitleBox = { width: totalW, height: maxH };
+        }
         else {
             const text = await getTextWithElipsis({
                 text: axis.title.text,
@@ -223,11 +298,12 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
                 size: await getTitleTextSize(text),
             });
         }
-        const originalTextSize = titleContent.reduce((acc, item) => {
-            acc.width = Math.max(acc.width, item.size.width);
-            acc.height += item.size.height;
-            return acc;
-        }, { width: 0, height: 0 });
+        const originalTextSize = passengerTitleBox ||
+            titleContent.reduce((acc, item) => {
+                acc.width = Math.max(acc.width, item.size.width);
+                acc.height += item.size.height;
+                return acc;
+            }, { width: 0, height: 0 });
         const rotatedTitleSize = {
             width: sin * originalTextSize.height + cos * originalTextSize.width,
             height: sin * originalTextSize.width + cos * originalTextSize.height,
@@ -252,6 +328,9 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
         const x = axis.position === 'left'
             ? -left - labelsWidth - axis.labels.margin - axis.title.margin
             : -left + width + labelsWidth + axis.labels.margin + axis.title.margin;
+        const titleOffset = passengerInlineTspans
+            ? 0
+            : -(originalTextSize.height / titleContent.length) * (titleContent.length - 1);
         title = {
             content: titleContent,
             style: axis.title.style,
@@ -259,7 +338,7 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
             x,
             y: axisPlotTopPosition + y,
             rotate: rotateAngle,
-            offset: -(originalTextSize.height / titleContent.length) * (titleContent.length - 1),
+            offset: titleOffset,
         };
     }
     const plotBands = [];
@@ -326,6 +405,23 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
             dashStyle: plotLine.dashStyle,
         });
     }
+    if (domain && ticks.length > 0) {
+        let topTickY = Infinity;
+        let bottomTickY = -Infinity;
+        for (const t of ticks) {
+            const ty = t.line ? t.line.points[0][1] : (t.svgLabel ? t.svgLabel.y : null);
+            if (ty !== null) {
+                topTickY = Math.min(topTickY, ty);
+                bottomTickY = Math.max(bottomTickY, ty);
+            }
+        }
+        if (topTickY !== Infinity) {
+            domain.start = [domain.start[0], topTickY];
+        }
+        if (bottomTickY !== -Infinity) {
+            domain.end = [domain.end[0], bottomTickY + 6];
+        }
+    }
     return {
         id: getUniqId(),
         title,
@@ -333,5 +429,9 @@ export async function prepareAxisData({ axis, split, scale, top: topOffset, widt
         domain,
         plotBands,
         plotLines,
+        tickLength: typeof axis.tickLength === 'number' ? axis.tickLength : undefined,
+        yTickLengthAtBottomPx: typeof axis.yTickLengthAtBottomPx === 'number'
+            ? axis.yTickLengthAtBottomPx
+            : undefined,
     };
 }
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/styles.css b/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/styles.css
index ae33c44..b3bdbf6 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/styles.css
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/styles.css
@@ -1,15 +1,35 @@
 .gcharts-y-axis__domain {
-  stroke: var(--g-color-line-generic-active);
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
 }
 .gcharts-y-axis__label {
-  fill: var(--g-color-text-secondary);
+  fill: #000000;
   stroke: none;
   dominant-baseline: text-after-edge;
+  text-rendering: optimizeSpeed;
+  font-weight: 400;
 }
 .gcharts-y-axis__tick {
-  stroke: var(--g-color-line-generic);
+  stroke: none;
+  shape-rendering: crispEdges;
 }
-.gcharts-y-axis__title {
-  dominant-baseline: text-after-edge;
-  fill: var(--g-color-text-secondary);
+.gcharts-y-axis__tick path {
+  stroke-width: 1;
+  shape-rendering: crispEdges;
+}
+.gcharts-y-axis__outer-tick {
+  stroke: #000000;
+  stroke-width: 1;
+  shape-rendering: crispEdges;
+}
+.gcharts-y-axis__title text {
+  fill: #000000;
+  stroke: none;
+  text-rendering: optimizeSpeed;
+  font-weight: 400;
+}
+.gcharts-y-axis__title tspan {
+  fill: #000000;
+  stroke: none;
 }
\ No newline at end of file
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/utils.js b/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/utils.js
index 0dd0e7b..6e6089e 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/utils.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/AxisY/utils.js
@@ -1,3 +1,4 @@
+import get from 'lodash/get';
 import { getDomainDataYBySeries, getMinSpaceBetween, getTicksCount, isBandScale } from '../../utils';
 function thinOut(items, delta) {
     const arr = [];
@@ -7,6 +8,30 @@ function thinOut(items, delta) {
     return arr;
 }
 export function getTickValues({ scale, axis, labelLineHeight, series, }) {
+    const ensureZeroTick = (items) => {
+        if (!items.length || axis.min !== 0) {
+            return items;
+        }
+        const hasZero = items.some((i) => Number(i.value) === 0);
+        if (hasZero) {
+            return items;
+        }
+        const zeroY = scale(0);
+        if (!Number.isFinite(zeroY)) {
+            return items;
+        }
+        const next = [...items, { y: zeroY, value: 0 }];
+        next.sort((a, b) => Number(a.value) - Number(b.value));
+        return next;
+    };
+    const fixedTicks = get(axis, 'tickPositions');
+    if (Array.isArray(fixedTicks) && fixedTicks.length && scale && typeof scale === 'function') {
+        const items = fixedTicks.map((t) => ({
+            y: scale(Number(t)),
+            value: Number(t),
+        }));
+        return ensureZeroTick(items);
+    }
     if ('ticks' in scale && typeof scale.ticks === 'function') {
         const range = scale.range();
         const height = Math.abs(range[0] - range[1]);
@@ -64,7 +89,7 @@ export function getTickValues({ scale, axis, labelLineHeight, series, }) {
                 }
             }
         }
-        return result;
+        return ensureZeroTick(result);
     }
     if (isBandScale(scale)) {
         const domain = scale.domain();
@@ -89,7 +114,7 @@ export function getTickValues({ scale, axis, labelLineHeight, series, }) {
                 availableSpaceForLabel = result[0].y - result[1].y - axis.labels.padding * 2;
             }
         }
-        return result;
+        return ensureZeroTick(result);
     }
     return [];
 }
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/ChartInner/index.js b/node_modules/@gravity-ui/charts/dist/esm/components/ChartInner/index.js
index 11c5982..0844759 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/ChartInner/index.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/ChartInner/index.js
@@ -116,6 +116,41 @@ export const ChartInner = (props) => {
         return items;
     }, [boundsHeight, boundsOffsetTop, boundsWidth, preparedSeries, preparedSplit, yAxis, yScale]);
     const yAxisDataItems = useAsyncState([], setYAxisDataItems);
+    // X-axis group was translate(0, boundsHeight); Y grid/ticks use snapTickY(plot.top + scale(domainMin))
+    // from prepare-axis-data. If those differ (D3 float / DPR snap), the baseline sits off the y=0 row.
+    const axisXTranslateY = React.useMemo(() => {
+        var _a;
+        const plot0 = ((_a = preparedSplit === null || preparedSplit === void 0 ? void 0 : preparedSplit.plots) === null || _a === void 0 ? void 0 : _a[0]) || { top: 0, height: boundsHeight };
+        const top = plot0.top || 0;
+        const plotH = typeof plot0.height === 'number' ? plot0.height : boundsHeight;
+        const dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+        const snapY = (raw) => {
+            const r = Math.round(raw);
+            return Math.round(r * dpr) / dpr;
+        };
+        const yS = yScale === null || yScale === void 0 ? void 0 : yScale[0];
+        let rawBottom = top + plotH;
+        if (yS && typeof yS.domain === 'function') {
+            try {
+                const dom = yS.domain();
+                if (dom && dom.length) {
+                    const n0 = Number(dom[0]);
+                    const n1 = Number(dom[dom.length - 1]);
+                    if (Number.isFinite(n0) && Number.isFinite(n1)) {
+                        const domainMin = Math.min(n0, n1);
+                        const mapped = yS(domainMin);
+                        if (typeof mapped === 'number' && Number.isFinite(mapped)) {
+                            rawBottom = top + mapped;
+                        }
+                    }
+                }
+            }
+            catch (_e) {
+                rawBottom = top + plotH;
+            }
+        }
+        return snapY(rawBottom);
+    }, [boundsHeight, preparedSplit, yScale]);
     return (React.createElement("div", { className: b() },
         React.createElement("svg", { ref: svgRef, width: width, height: height,
             // We use onPointerMove here because onMouseMove works incorrectly when the zoom setting is enabled:
@@ -124,13 +159,21 @@ export const ChartInner = (props) => {
             React.createElement("defs", null,
                 React.createElement("clipPath", { id: clipPathId },
                     React.createElement("rect", { x: 0, y: 0, width: boundsWidth, height: boundsHeight }))),
-            title && React.createElement(Title, Object.assign({}, title, { chartWidth: width })),
+            title &&
+                React.createElement(Title, Object.assign({}, title, {
+                    chartWidth: boundsWidth +
+                        (((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.position) === 'right'
+                            ? ((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.panelWidth) || 0) + ((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.margin) || 0)
+                            : 0)),
+                    chartOffsetLeft: boundsOffsetLeft + 54,
+                    titleOffsetTop: 4,
+                })),
             React.createElement("g", { transform: `translate(0, ${boundsOffsetTop})` }, preparedSplit.plots.map((plot, index) => {
                 return React.createElement(PlotTitle, { key: `plot-${index}`, title: plot.title });
             })),
             React.createElement("g", { width: boundsWidth, height: boundsHeight, transform: `translate(${[boundsOffsetLeft, boundsOffsetTop].join(',')})`, ref: plotRef },
-                xScale && xAxis && (React.createElement("g", { transform: `translate(0, ${boundsHeight})` },
-                    React.createElement(AxisX, { axis: xAxis, boundsOffsetLeft: boundsOffsetLeft, boundsOffsetTop: boundsOffsetTop, height: boundsHeight, htmlLayout: htmlLayout, leftmostLimit: svgXPos, plotAfterRef: plotAfterRef, plotBeforeRef: plotBeforeRef, scale: xScale, split: preparedSplit, width: boundsWidth }))),
+                xScale && xAxis && (React.createElement("g", { transform: `translate(0, ${axisXTranslateY})` },
+                    React.createElement(AxisX, { axis: xAxis, boundsOffsetLeft: boundsOffsetLeft, boundsOffsetTop: boundsOffsetTop, height: boundsHeight, htmlLayout: htmlLayout, leftmostLimit: svgXPos, plotAfterRef: plotAfterRef, plotBeforeRef: plotBeforeRef, scale: xScale, split: preparedSplit, width: boundsWidth, xAxisBaselineY: axisXTranslateY }))),
                 Boolean(yAxisDataItems.length) && (React.createElement(React.Fragment, null, yAxisDataItems.map((axisData, index) => {
                     if (!axisData) {
                         return null;
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/ChartInner/useChartInnerProps.js b/node_modules/@gravity-ui/charts/dist/esm/components/ChartInner/useChartInnerProps.js
index cdf130d..da93c13 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/ChartInner/useChartInnerProps.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/ChartInner/useChartInnerProps.js
@@ -125,9 +125,9 @@ export function useChartInnerProps(props) {
         yAxis,
         yScale,
     });
-    const boundsOffsetTop = chart.margin.top;
+    const boundsOffsetTop = Math.round(chart.margin.top);
     // We need to calculate the width of each left axis because the first axis can be hidden
-    const boundsOffsetLeft = chart.margin.left +
+    const boundsOffsetLeft = Math.round(chart.margin.left +
         yAxis.reduce((acc, axis) => {
             if (axis.position !== 'left') {
                 return acc;
@@ -137,7 +137,7 @@ export function useChartInnerProps(props) {
                 acc = axisWidth;
             }
             return acc;
-        }, 0);
+        }, 0));
     const { x } = (_a = svgContainer === null || svgContainer === void 0 ? void 0 : svgContainer.getBoundingClientRect()) !== null && _a !== void 0 ? _a : {};
     return {
         svgXPos: x,
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/Legend/index.js b/node_modules/@gravity-ui/charts/dist/esm/components/Legend/index.js
index ba17740..bf030e1 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/Legend/index.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/Legend/index.js
@@ -236,6 +236,7 @@ export const Legend = (props) => {
                             return ('name' in d && d.name);
                         })
                             .style('font-size', legend.itemStyle.fontSize)
+                            .style('fill', legend.itemStyle.fontColor || null)
                             .each((d, index, nodes) => {
                             if (d.overflowed) {
                                 handleOverflowingText(nodes[index], d.textWidth);
@@ -263,7 +264,7 @@ export const Legend = (props) => {
                             break;
                         }
                     }
-                    const top = legendLineHeights.reduce((acc, h) => acc + h, 0);
+                    const top = legendLineHeights.reduce((acc, h) => acc + h, 0) + legendLineHeights.length * 2;
                     legendLineHeights.push(legendLineHeight);
                     legendLine.attr('transform', `translate(${[left, top].join(',')})`);
                     htmlLegendLine === null || htmlLegendLine === void 0 ? void 0 : htmlLegendLine.style('transform', `translate(${left}px, ${top}px)`);
diff --git a/node_modules/@gravity-ui/charts/dist/esm/components/Title/index.js b/node_modules/@gravity-ui/charts/dist/esm/components/Title/index.js
index f16e541..9d6117f 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/components/Title/index.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/components/Title/index.js
@@ -1,7 +1,7 @@
 import React from 'react';
 export const Title = (props) => {
-    const { chartWidth, text, height, style } = props;
-    return (React.createElement("text", { dx: chartWidth / 2, dy: height / 2, dominantBaseline: "middle", textAnchor: "middle", style: {
+    const { chartWidth, chartOffsetLeft, titleOffsetTop, text, height, style } = props;
+    return (React.createElement("text", { dx: (chartOffsetLeft || 0) + chartWidth / 2, dy: height / 2 + (titleOffsetTop || 0), dominantBaseline: "middle", textAnchor: "middle", style: {
             fill: style === null || style === void 0 ? void 0 : style.fontColor,
             fontSize: style === null || style === void 0 ? void 0 : style.fontSize,
             fontWeight: style === null || style === void 0 ? void 0 : style.fontWeight,
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useAxis/index.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useAxis/index.js
index 7e733e8..5d3af29 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useAxis/index.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useAxis/index.js
@@ -1,10 +1,48 @@
 import React from 'react';
 import isEqual from 'lodash/isEqual';
+import { isAxisRelatedSeries } from '../../utils';
 import { getPreparedXAxis } from '../useChartOptions/x-axis';
 import { getPreparedYAxis } from '../useChartOptions/y-axis';
+
+/**
+ * Должна совпадать с useChartDimensions/getBottomOffset + формула boundsHeight,
+ * иначе getPreparedYAxis измеряет подписи по одной высоте, а шкала/линии — по другой.
+ */
+function estimateBoundsHeightForYAxisPreparation(args) {
+    const { height, preparedChart, preparedLegend, preparedXAxis, preparedSeries } = args;
+    const hasAxisRelatedSeries = preparedSeries.some(isAxisRelatedSeries);
+    let bottomOffset = 0;
+    if ((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) &&
+        preparedLegend.position !== 'right') {
+        bottomOffset += preparedLegend.height + preparedLegend.margin;
+    }
+    if (!(preparedXAxis === null || preparedXAxis === void 0 ? void 0 : preparedXAxis.visible)) {
+        return Math.round(height - preparedChart.margin.top - preparedChart.margin.bottom - bottomOffset);
+    }
+    if (hasAxisRelatedSeries) {
+        if (preparedXAxis.title.text) {
+            bottomOffset += preparedXAxis.title.height + preparedXAxis.title.margin;
+        }
+        if (preparedXAxis.labels.enabled) {
+            bottomOffset += preparedXAxis.labels.margin + preparedXAxis.labels.height;
+        }
+    }
+    return Math.round(height - preparedChart.margin.top - preparedChart.margin.bottom - bottomOffset);
+}
+
 export function useAxis(props) {
-    const { boundsHeight, height, preparedChart, preparedLegend, preparedSeries, preparedSeriesOptions, width, xAxis, yAxis, } = props;
-    const [axesState, setValue] = React.useState({ xAxis: null, yAxis: [] });
+    const {
+        boundsHeight,
+        height,
+        preparedChart,
+        preparedLegend,
+        preparedSeries,
+        preparedSeriesOptions,
+        width,
+        xAxis,
+        yAxis,
+    } = props;
+    const [axesState, setValue] = React.useState({xAxis: null, yAxis: []});
     const axesStateRunRef = React.useRef(0);
     const prevAxesStateValue = React.useRef(axesState);
     const axesStateReady = React.useRef(false);
@@ -20,18 +58,16 @@ export function useAxis(props) {
                 seriesData,
                 seriesOptions: preparedSeriesOptions,
             });
-            let estimatedBoundsHeight = boundsHeight !== null && boundsHeight !== void 0 ? boundsHeight : height;
-            if (preparedXAxis && typeof boundsHeight !== 'number') {
-                estimatedBoundsHeight =
-                    height -
-                        (preparedXAxis.title.height +
-                            preparedXAxis.title.margin +
-                            preparedXAxis.labels.margin +
-                            preparedXAxis.labels.height +
-                            (preparedLegend ? preparedLegend.height + preparedLegend.margin : 0) +
-                            preparedChart.margin.top +
-                            preparedChart.margin.bottom);
-            }
+            const estimatedBoundsHeight =
+                typeof boundsHeight === 'number'
+                    ? boundsHeight
+                    : estimateBoundsHeightForYAxisPreparation({
+                          height,
+                          preparedChart,
+                          preparedLegend,
+                          preparedXAxis,
+                          preparedSeries,
+                      });
             const preparedYAxis = await getPreparedYAxis({
                 height,
                 boundsHeight: estimatedBoundsHeight,
@@ -39,7 +75,7 @@ export function useAxis(props) {
                 seriesData,
                 yAxis,
             });
-            const newStateValue = { xAxis: preparedXAxis, yAxis: preparedYAxis };
+            const newStateValue = {xAxis: preparedXAxis, yAxis: preparedYAxis};
             if (axesStateRunRef.current === currentRun) {
                 if (!isEqual(prevAxesStateValue.current, newStateValue)) {
                     setValue(newStateValue);
@@ -59,5 +95,5 @@ export function useAxis(props) {
         xAxis,
         yAxis,
     ]);
-    return axesStateReady.current ? axesState : { xAxis: null, yAxis: [] };
+    return axesStateReady.current ? axesState : {xAxis: null, yAxis: []};
 }
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useAxisScales/index.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useAxisScales/index.js
index 6f4194f..6bdd37e 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useAxisScales/index.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useAxisScales/index.js
@@ -1,5 +1,5 @@
 import React from 'react';
-import { extent, scaleBand, scaleLinear, scaleLog, scaleUtc } from 'd3';
+import { extent, scaleBand, scaleLinear, scaleLog, scalePoint, scaleUtc } from 'd3';
 import get from 'lodash/get';
 import { DEFAULT_AXIS_TYPE, SeriesType } from '../../constants';
 import { CHART_SERIES_WITH_VOLUME_ON_Y_AXIS, getAxisHeight, getDataCategoryValue, getDefaultMaxXAxisValue, getDefaultMinXAxisValue, getDomainDataXBySeries, getDomainDataYBySeries, getOnlyVisibleSeries, isAxisRelatedSeries, isSeriesWithCategoryValues, } from '../../utils';
@@ -26,11 +26,20 @@ function validateArrayData(data) {
 }
 function filterCategoriesByVisibleSeries(args) {
     const { axisDirection, categories, series } = args;
+    if (axisDirection === 'x') {
+        const axisSeries = series.filter(isAxisRelatedSeries);
+        if (axisSeries.length > 0 && axisSeries.every((s) => s.type === SeriesType.Line)) {
+            return categories;
+        }
+    }
     const visibleCategories = new Set();
     series.forEach((s) => {
         if (isSeriesWithCategoryValues(s)) {
             s.data.forEach((d) => {
-                visibleCategories.add(getDataCategoryValue({ axisDirection, categories, data: d }));
+                const val = getDataCategoryValue({ axisDirection, categories, data: d });
+                if (val !== null && val !== undefined) {
+                    visibleCategories.add(val);
+                }
             });
         }
     });
@@ -107,7 +116,15 @@ export function createYScale(args) {
                 }
                 const domainOffsetMin = Math.abs(scale.invert(offsetMin) - scale.invert(0));
                 const domainOffsetMax = Math.abs(scale.invert(offsetMax) - scale.invert(0));
-                return scale.domain([yMin - domainOffsetMin, yMax + domainOffsetMax]);
+                const effectiveMax = yMax + domainOffsetMax;
+                const niceHint = effectiveMax > 120 ? 3 : 7;
+                // DataLens: явный max + tickPositions — не расширять .nice(), иначе 110→120 и «лишний» шаг сетки.
+                const skipNice = get(axis, 'fixedNumericDomain') === true ||
+                    (Array.isArray(get(axis, 'tickPositions')) && get(axis, 'tickPositions').length > 0);
+                if (skipNice) {
+                    return scale.domain([yMin - domainOffsetMin, effectiveMax]);
+                }
+                return scale.domain([yMin - domainOffsetMin, effectiveMax]).nice(niceHint);
             }
             break;
         }
@@ -278,6 +295,12 @@ export function createXScale(args) {
                 if (!hasZoomX && !hasOffset) {
                     // 10 is the default value for the number of ticks. Here, to preserve the appearance of a series with a small number of points
                     scale.nice(Math.max(10, domainData.length));
+                    // Restore explicitly set min/max that nice() may have rounded away
+                    const [nicedMin, nicedMax] = scale.domain();
+                    scale.domain([
+                        typeof xMinProps === 'number' ? xMinProps : nicedMin,
+                        typeof xMaxProps === 'number' ? xMaxProps : nicedMax,
+                    ]);
                 }
                 return scale;
             }
@@ -290,11 +313,22 @@ export function createXScale(args) {
                     categories: xCategories,
                     series: series,
                 });
-                const xScale = scaleBand().domain(filteredCategories).range([0, boundsWidth]);
-                if (xScale.step() / 2 < xAxisMaxPadding) {
-                    xScale.range(range);
+                const axisSeries = series.filter(isAxisRelatedSeries);
+                const pointScaleSeriesTypes = new Set([SeriesType.Line, SeriesType.Area]);
+                const useCategoryPointScale = filteredCategories.length >= 2 &&
+                    axisSeries.length > 0 &&
+                    axisSeries.every((s) => pointScaleSeriesTypes.has(s.type));
+                if (useCategoryPointScale) {
+                    return scalePoint()
+                        .domain(filteredCategories)
+                        .range([0, boundsWidth])
+                        .padding(0);
                 }
-                return xScale;
+                return scaleBand()
+                    .domain(filteredCategories)
+                    .range([0, boundsWidth])
+                    .paddingInner(0)
+                    .paddingOuter(0);
             }
             break;
         }
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartDimensions/index.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartDimensions/index.js
index 9bedeb9..a948dd0 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartDimensions/index.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartDimensions/index.js
@@ -5,7 +5,8 @@ export { getBoundsWidth } from './utils';
 const getBottomOffset = (args) => {
     const { hasAxisRelatedSeries, preparedLegend, preparedXAxis } = args;
     let result = 0;
-    if (preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) {
+    // Right-panel legend does not consume vertical space at the bottom
+    if ((preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) && preparedLegend.position !== 'right') {
         result += preparedLegend.height + preparedLegend.margin;
     }
     if (!(preparedXAxis === null || preparedXAxis === void 0 ? void 0 : preparedXAxis.visible)) {
@@ -25,13 +26,19 @@ export const useChartDimensions = (args) => {
     const { margin, width, height, preparedLegend, preparedXAxis, preparedYAxis, preparedSeries } = args;
     return React.useMemo(() => {
         const hasAxisRelatedSeries = preparedSeries.some(isAxisRelatedSeries);
-        const boundsWidth = getBoundsWidth({ chartWidth: width, chartMargin: margin, preparedYAxis });
+        // When legend is a right panel, subtract its width + margin from the plot area
+        const legendPanelReserve =
+            (preparedLegend === null || preparedLegend === void 0 ? void 0 : preparedLegend.enabled) &&
+            preparedLegend.position === 'right'
+                ? preparedLegend.panelWidth + preparedLegend.margin
+                : 0;
+        const boundsWidth = getBoundsWidth({ chartWidth: width, chartMargin: margin, preparedYAxis }) - legendPanelReserve;
         const bottomOffset = getBottomOffset({
             hasAxisRelatedSeries,
             preparedLegend,
             preparedXAxis,
         });
-        const boundsHeight = height - margin.top - margin.bottom - bottomOffset;
-        return { boundsWidth, boundsHeight };
+        const boundsHeight = Math.round(height - margin.top - margin.bottom - bottomOffset);
+        return { boundsWidth: Math.round(boundsWidth), boundsHeight };
     }, [margin, width, height, preparedLegend, preparedXAxis, preparedYAxis, preparedSeries]);
 };
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartOptions/x-axis.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartOptions/x-axis.js
index b78b24a..9235baa 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartOptions/x-axis.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartOptions/x-axis.js
@@ -75,6 +75,7 @@ export const getPreparedXAxis = async ({ xAxis, seriesData, seriesOptions, width
     });
     const labelsStyle = {
         fontSize: get(xAxis, 'labels.style.fontSize', DEFAULT_AXIS_LABEL_FONT_SIZE),
+        fontWeight: get(xAxis, 'labels.style.fontWeight', '400'),
     };
     const labelsHtml = get(xAxis, 'labels.html', false);
     const labelsLineHeight = labelsHtml
@@ -90,6 +91,9 @@ export const getPreparedXAxis = async ({ xAxis, seriesData, seriesOptions, width
             dateFormat: get(xAxis, 'labels.dateFormat'),
             numberFormat: get(xAxis, 'labels.numberFormat'),
             rotation: get(xAxis, 'labels.rotation', 0),
+            align: get(xAxis, 'labels.align'),
+            x: get(xAxis, 'labels.x'),
+            y: get(xAxis, 'labels.y'),
             style: labelsStyle,
             width: 0,
             height: 0,
@@ -153,6 +157,17 @@ export const getPreparedXAxis = async ({ xAxis, seriesData, seriesOptions, width
         },
         visible: get(xAxis, 'visible', true),
         order: xAxis === null || xAxis === void 0 ? void 0 : xAxis.order,
+        tickLength: get(xAxis, 'tickLength'),
+        tickPosition: get(xAxis, 'tickPosition'),
+        lineWidth: get(xAxis, 'lineWidth'),
+        omitCornerVerticalTick: get(xAxis, 'omitCornerVerticalTick'),
+        zeroLabelVerticalNudgePx: get(xAxis, 'zeroLabelVerticalNudgePx'),
+        zeroLabelHNudgePx: get(xAxis, 'zeroLabelHNudgePx'),
+        xTickLengthAtRightPx: get(xAxis, 'xTickLengthAtRightPx'),
+        xDomainExtendRightPx: get(xAxis, 'xDomainExtendRightPx'),
+        startOnTick: get(xAxis, 'startOnTick'),
+        endOnTick: get(xAxis, 'endOnTick'),
+        minPadding: get(xAxis, 'minPadding'),
     };
     await setLabelSettings({
         axis: preparedXAxis,
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartOptions/y-axis.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartOptions/y-axis.js
index da84daf..5878eff 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartOptions/y-axis.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useChartOptions/y-axis.js
@@ -63,6 +63,7 @@ export const getPreparedYAxis = ({ height, boundsHeight, width, seriesData, yAxi
         const labelsEnabled = get(axisItem, 'labels.enabled', true);
         const labelsStyle = {
             fontSize: get(axisItem, 'labels.style.fontSize', DEFAULT_AXIS_LABEL_FONT_SIZE),
+            fontWeight: get(axisItem, 'labels.style.fontWeight', '400'),
         };
         const labelsHtml = get(axisItem, 'labels.html', false);
         const labelsLineHeight = labelsHtml
@@ -97,6 +98,7 @@ export const getPreparedYAxis = ({ height, boundsHeight, width, seriesData, yAxi
                 lineHeight: labelsLineHeight,
                 maxWidth: (_b = calculateNumericProperty({ base: width, value: (_a = axisItem.labels) === null || _a === void 0 ? void 0 : _a.maxWidth })) !== null && _b !== void 0 ? _b : axisLabelsDefaults.maxWidth,
                 html: labelsHtml,
+                y: get(axisItem, 'labels.y', 0),
             },
             lineColor: get(axisItem, 'lineColor'),
             categories: axisItem.categories,
@@ -112,6 +114,8 @@ export const getPreparedYAxis = ({ height, boundsHeight, width, seriesData, yAxi
             },
             min: (_c = get(axisItem, 'min')) !== null && _c !== void 0 ? _c : getDefaultMinYAxisValue(axisSeriesData),
             max: get(axisItem, 'max'),
+            tickPositions: get(axisItem, 'tickPositions'),
+            fixedNumericDomain: get(axisItem, 'fixedNumericDomain'),
             maxPadding: get(axisItem, 'maxPadding', getMaxPaddingBySeries({ series: axisSeriesData })),
             grid: {
                 enabled: shouldHideGrid
@@ -127,6 +131,8 @@ export const getPreparedYAxis = ({ height, boundsHeight, width, seriesData, yAxi
                     })
                     : (_g = axisItem.ticks) === null || _g === void 0 ? void 0 : _g.pixelInterval,
             },
+            tickLength: get(axisItem, 'tickLength'),
+            yTickLengthAtBottomPx: get(axisItem, 'yTickLengthAtBottomPx'),
             position: get(axisItem, 'position', defaultAxisPosition),
             plotIndex: get(axisItem, 'plotIndex', 0),
             plotLines: get(axisItem, 'plotLines', []).map((d) => ({
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/prepare-legend.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/prepare-legend.js
index 6ac1679..8aaabbc 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/prepare-legend.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/prepare-legend.js
@@ -49,9 +49,11 @@ export async function getPreparedLegend(args) {
         }
     }
     const legendWidth = get(legend, 'width', CONTINUOUS_LEGEND_SIZE.width);
+    const legendPosition = get(legend, 'position', 'bottom');
+    const legendPanelWidth = legendPosition === 'right' ? get(legend, 'width', 160) : legendWidth;
     return {
-        align: get(legend, 'align', legendDefaults.align),
-        justifyContent: get(legend, 'justifyContent', legendDefaults.justifyContent),
+        align: legendPosition === 'right' ? 'left' : get(legend, 'align', legendDefaults.align),
+        justifyContent: legendPosition === 'right' ? 'start' : get(legend, 'justifyContent', legendDefaults.justifyContent),
         enabled,
         height,
         itemDistance: get(legend, 'itemDistance', legendDefaults.itemDistance),
@@ -59,6 +61,8 @@ export async function getPreparedLegend(args) {
         lineHeight,
         margin: get(legend, 'margin', legendDefaults.margin),
         type: legendType,
+        position: legendPosition,
+        panelWidth: legendPanelWidth,
         title: {
             enable: isTitleEnabled,
             text: titleText,
@@ -165,8 +169,66 @@ function getPagination(args) {
     });
     return { pages };
 }
+function getVerticalLegendItems(items, preparedLegend, panelWidth) {
+    const result = [];
+    const bodySelection = select(document.body);
+    items.forEach((item) => {
+        const itemSelection = preparedLegend.html
+            ? bodySelection
+                .append('div')
+                .html(item.name)
+                .style('position', 'absolute')
+                .style('display', 'inline-block')
+                .style('white-space', 'nowrap')
+            : bodySelection.append('text').text(item.name).style('white-space', 'nowrap');
+        itemSelection.style('font-size', preparedLegend.itemStyle.fontSize).each(function () {
+            const resultItem = clone(item);
+            const { height, width: textWidth } = this.getBoundingClientRect();
+            resultItem.height = height || preparedLegend.lineHeight;
+            const maxTextWidth = panelWidth - resultItem.symbol.width - resultItem.symbol.padding - 4;
+            if (textWidth > maxTextWidth) {
+                resultItem.overflowed = true;
+                resultItem.textWidth = maxTextWidth;
+            } else {
+                resultItem.textWidth = textWidth;
+            }
+            result.push([resultItem]);
+        }).remove();
+    });
+    return result;
+}
 export function getLegendComponents(args) {
     const { chartWidth, chartHeight, chartMargin, series, preparedLegend } = args;
+
+    if (preparedLegend.position === 'right' && preparedLegend.enabled) {
+        const panelWidth = preparedLegend.panelWidth;
+        const flattenItems = getFlattenLegendItems(series, preparedLegend);
+        const items = getVerticalLegendItems(flattenItems, preparedLegend, panelWidth);
+        const maxLegendHeight = chartHeight - chartMargin.top - chartMargin.bottom;
+        let pagination;
+        if (preparedLegend.type === 'discrete') {
+            const totalHeight = items.reduce((acc, row) => {
+                return acc + (row.length ? Math.max(...row.map(({ height }) => height)) : 0);
+            }, 0);
+            preparedLegend.height = Math.min(totalHeight, maxLegendHeight);
+            if (totalHeight > maxLegendHeight) {
+                pagination = getPagination({
+                    items,
+                    maxLegendHeight: maxLegendHeight - preparedLegend.lineHeight,
+                    paginatorHeight: preparedLegend.lineHeight,
+                });
+            }
+        }
+        const offset = {
+            left: chartWidth - chartMargin.right - panelWidth + 52,
+            top: chartMargin.top,
+        };
+        return {
+            legendConfig: { offset, pagination, maxWidth: panelWidth, position: 'right' },
+            legendItems: items,
+        };
+    }
+
     const maxLegendWidth = chartWidth - chartMargin.right - chartMargin.left;
     const maxLegendHeight = (chartHeight - chartMargin.top - chartMargin.bottom - preparedLegend.margin) / 2;
     const flattenLegendItems = getFlattenLegendItems(series, preparedLegend);
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/prepare-line.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/prepare-line.js
index 9902ea9..2f23f9b 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/prepare-line.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/prepare-line.js
@@ -92,6 +92,11 @@ export function prepareLineSeries(args) {
             cursor: get(series, 'cursor', null),
             yAxis: get(series, 'yAxis', 0),
             tooltip: series.tooltip,
+            // DataLens / upstream: server may set crisp|clip|zIndex on line series; do not drop them (used by LineSeriesShapes).
+            ...(typeof series.crisp === 'boolean' ? { crisp: series.crisp } : {}),
+            ...(typeof series.clip === 'boolean' ? { clip: series.clip } : {}),
+            ...(typeof series.zIndex === 'number' && Number.isFinite(series.zIndex) ? { zIndex: series.zIndex } : {}),
+            ...(series.states ? { states: series.states } : {}),
         };
         return prepared;
     }, []);
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/utils.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/utils.js
index e6a6af8..8259087 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/utils.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useSeries/utils.js
@@ -17,9 +17,11 @@ export function prepareLegendSymbol(series, symbolType) {
     var _a;
     const symbolOptions = ((_a = series.legend) === null || _a === void 0 ? void 0 : _a.symbol) || {};
     return {
-        shape: 'symbol',
-        symbolType: symbolType || SymbolType.Circle,
+        shape: symbolOptions.shape || 'symbol',
+        symbolType: symbolType || symbolOptions.symbolType || SymbolType.Circle,
         width: (symbolOptions === null || symbolOptions === void 0 ? void 0 : symbolOptions.width) || DEFAULT_LEGEND_SYMBOL_SIZE,
+        height: symbolOptions.height,
+        radius: symbolOptions.radius,
         padding: (symbolOptions === null || symbolOptions === void 0 ? void 0 : symbolOptions.padding) || DEFAULT_LEGEND_SYMBOL_PADDING,
     };
 }
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/line/index.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/line/index.js
index c58d313..5cefcdf 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/line/index.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/line/index.js
@@ -26,17 +26,91 @@ export const LineSeriesShapes = (args) => {
             .y((d) => d.y);
         plotSvgElement.selectAll('*').remove();
         markersSvgElement.selectAll('*').remove();
+        const dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+        const pp = (v) => Math.round(v * dpr) / dpr;
+        const ps = 1 / dpr;
+        // Empirical: path Y from snapLinePathY vs AxisY grid/tick rects — center band + tiny shift down.
+        const hCrispBandYAlignPx = 0.35;
+        const Y_FLAT_EPS = 1e-3;
+        const isHorizontalCrisp = (seriesPrepared) => {
+            var _a;
+            const crisp = Boolean((_a = seriesPrepared === null || seriesPrepared === void 0 ? void 0 : seriesPrepared.series) === null || _a === void 0 ? void 0 : _a.crisp);
+            if (!crisp) {
+                return false;
+            }
+            const points = (seriesPrepared === null || seriesPrepared === void 0 ? void 0 : seriesPrepared.points) || [];
+            if (points.length < 2) {
+                return false;
+            }
+            const ys = points
+                .map((p) => p.y)
+                .filter((y) => typeof y === 'number' && Number.isFinite(y));
+            if (ys.length < 2) {
+                return false;
+            }
+            const minY = Math.min(...ys);
+            const maxY = Math.max(...ys);
+            return maxY - minY < Y_FLAT_EPS;
+        };
+        const hBandData = preparedData.filter(isHorizontalCrisp);
+        const pathData = preparedData.filter((d) => !isHorizontalCrisp(d));
         const lineSelection = plotSvgElement
-            .selectAll('path')
-            .data(preparedData)
+            .selectAll(`path.${b('path-geom')}`)
+            .data(pathData, (d) => d.id)
             .join('path')
+            .attr('class', b('path-geom'))
+            .attr('data-series-line-id', (d) => String(d.id))
             .attr('d', (d) => line(d.points))
             .attr('fill', 'none')
             .attr('stroke', (d) => d.color)
             .attr('stroke-width', (d) => d.width)
-            .attr('stroke-linejoin', (d) => d.linecap)
-            .attr('stroke-linecap', (d) => d.linecap)
+            // Keep line ends flush at the plot edge to avoid crossing Y-axis.
+            .attr('stroke-linejoin', 'miter')
+            .attr('stroke-linecap', 'butt')
             .attr('stroke-dasharray', (d) => getLineDashArray(d.dashStyle, d.width))
+            .attr('shape-rendering', null)
+            .attr('opacity', (d) => d.opacity)
+            .attr('cursor', (d) => d.series.cursor);
+        // After paths so bands paint above data (matches high zIndex threshold series).
+        // Same geometry as AxisY grid: filled rect y=_pp(Math.round(y)), height=1/dpr.
+        const crispBandRects = plotSvgElement
+            .selectAll(`rect.${b('y-grid-band')}`)
+            .data(hBandData, (d) => d.id)
+            .join('rect')
+            .attr('class', b('y-grid-band'))
+            .attr('data-series-line-id', (d) => String(d.id))
+            .attr('x', (d) => {
+            const xs = d.points.map((p) => p.x).filter((x) => typeof x === 'number' && Number.isFinite(x));
+            return xs.length ? Math.min(...xs) : 0;
+        })
+            .attr('width', (d) => {
+            const xs = d.points.map((p) => p.x).filter((x) => typeof x === 'number' && Number.isFinite(x));
+            if (!xs.length) {
+                return 0;
+            }
+            const w = Math.max(...xs) - Math.min(...xs);
+            return Number.isFinite(w) ? Math.max(0, w) : 0;
+        })
+            // Grid band is [top0, top0+ps]; center thick fill on that band (symmetric up/down).
+            .attr('y', (d) => {
+            const ys = d.points
+                .map((p) => p.y)
+                .filter((y) => typeof y === 'number' && Number.isFinite(y));
+            const rawY = ys.length ? ys.reduce((a, b) => a + b, 0) / ys.length : d.points[0].y;
+            const lw = Number(d.width);
+            const linePx = Number.isFinite(lw) && lw > 0 ? lw : 1;
+            const h = Math.max(ps, linePx);
+            const top0 = pp(Math.round(rawY));
+            return top0 + ps / 2 - h / 2 + hCrispBandYAlignPx;
+        })
+            .attr('height', (d) => {
+            const lw = Number(d.width);
+            const linePx = Number.isFinite(lw) && lw > 0 ? lw : 1;
+            return Math.max(ps, linePx);
+        })
+            .attr('fill', (d) => d.color)
+            .style('stroke', 'none')
+            .attr('shape-rendering', 'crispEdges')
             .attr('opacity', (d) => d.opacity)
             .attr('cursor', (d) => d.series.cursor);
         let dataLabels = preparedData.reduce((acc, d) => {
@@ -70,19 +144,42 @@ export const LineSeriesShapes = (args) => {
             const selected = (data === null || data === void 0 ? void 0 : data.filter((d) => d.series.type === 'line')) || [];
             const selectedDataItems = selected.map((d) => d.data);
             const selectedSeriesIds = selected.map((d) => { var _a; return (_a = d.series) === null || _a === void 0 ? void 0 : _a.id; });
+            const applyHoverPaint = (elementSelection, dSelection) => {
+                var _a;
+                const initialColor = dSelection.color || '';
+                const nextColor = dSelection.hovered
+                    ? (((_a = color(initialColor)) === null || _a === void 0 ? void 0 : _a.brighter(hoverOptions === null || hoverOptions === void 0 ? void 0 : hoverOptions.brightness).toString()) || initialColor)
+                    : initialColor;
+                const n = elementSelection.node();
+                if (n && String(n.tagName).toLowerCase() === 'rect') {
+                    elementSelection.attr('fill', nextColor);
+                }
+                else {
+                    elementSelection.attr('stroke', nextColor);
+                }
+            };
             lineSelection.datum((d, index, list) => {
                 const elementSelection = select(list[index]);
                 const hovered = Boolean(hoverEnabled && selectedSeriesIds.includes(d.id));
                 if (d.hovered !== hovered) {
                     d.hovered = hovered;
-                    elementSelection.attr('stroke', (dSelection) => {
-                        var _a;
-                        const initialColor = dSelection.color || '';
-                        if (dSelection.hovered) {
-                            return (((_a = color(initialColor)) === null || _a === void 0 ? void 0 : _a.brighter(hoverOptions === null || hoverOptions === void 0 ? void 0 : hoverOptions.brightness).toString()) || initialColor);
-                        }
-                        return initialColor;
-                    });
+                    elementSelection.call((sel) => applyHoverPaint(sel, d));
+                }
+                return setActiveState({
+                    element: list[index],
+                    state: inactiveOptions,
+                    active: Boolean(!inactiveEnabled ||
+                        !selectedSeriesIds.length ||
+                        selectedSeriesIds.includes(d.id)),
+                    datum: d,
+                });
+            });
+            crispBandRects.datum((d, index, list) => {
+                const elementSelection = select(list[index]);
+                const hovered = Boolean(hoverEnabled && selectedSeriesIds.includes(d.id));
+                if (d.hovered !== hovered) {
+                    d.hovered = hovered;
+                    elementSelection.call((sel) => applyHoverPaint(sel, d));
                 }
                 return setActiveState({
                     element: list[index],
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/line/prepare-data.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/line/prepare-data.js
index 716b6bb..6099d7a 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/line/prepare-data.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/line/prepare-data.js
@@ -1,6 +1,16 @@
-import { getLabelsSize, getLeftPosition } from '../../../utils';
+import get from 'lodash/get';
+import { getDataCategoryValue, getLabelsSize, getLeftPosition } from '../../../utils';
 import { getFormattedValue } from '../../../utils/chart/format';
 import { getXValue, getYValue } from '../utils';
+/** Same physical-pixel snap as Y grid rects in AxisY.js (_pp ∘ Math.round) — avoids threshold lines sitting between grid rows. */
+function snapLinePathY(y) {
+    if (typeof y !== 'number' || !Number.isFinite(y)) {
+        return y;
+    }
+    const dpr = (typeof window !== 'undefined' && window.devicePixelRatio) || 1;
+    const rounded = Math.round(y);
+    return Math.round(rounded * dpr) / dpr;
+}
 async function getLabelData(point, series, xMax) {
     const text = getFormattedValue(Object.assign({ value: point.data.label || point.data.y }, series.dataLabels));
     const style = series.dataLabels.style;
@@ -60,9 +70,10 @@ export const prepareLineData = async (args) => {
                 yAxis: seriesYAxis,
                 yScale: seriesYScale,
             });
+            const x = getXValue({ point: d, points: s.data, xAxis, xScale });
             return {
-                x: getXValue({ point: d, points: s.data, xAxis, xScale }),
-                y: yValue === null ? null : yAxisTop + yValue,
+                x,
+                y: yValue === null ? null : snapLinePathY(yAxisTop + yValue),
                 active: true,
                 data: d,
                 series: s,
@@ -125,3 +136,4 @@ export const prepareLineData = async (args) => {
     }
     return acc;
 };
+
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/pie/index.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/pie/index.js
index 10f395f..4500a66 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/pie/index.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/pie/index.js
@@ -42,6 +42,11 @@ export function PieSeriesShapes(args) {
             return [];
         })
             .join('path')
+            .attr('transform', (d) => {
+            const dx = d.explodeDx || 0;
+            const dy = d.explodeDy || 0;
+            return dx || dy ? `translate(${dx}, ${dy})` : null;
+        })
             .attr('d', (d) => {
             const arcGenerator = arc()
                 .innerRadius(d.data.pie.innerRadius)
@@ -59,6 +64,11 @@ export function PieSeriesShapes(args) {
             .selectAll(segmentSelector)
             .data((pieData) => pieData.segments)
             .join('path')
+            .attr('transform', (d) => {
+            const dx = d.explodeDx || 0;
+            const dy = d.explodeDy || 0;
+            return dx || dy ? `translate(${dx}, ${dy})` : null;
+        })
             .attr('d', (d) => {
             const arcGenerator = arc()
                 .innerRadius(d.data.pie.innerRadius)
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/pie/prepare-data.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/pie/prepare-data.js
index d3f2ffa..f064dbb 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/pie/prepare-data.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/pie/prepare-data.js
@@ -78,7 +78,21 @@ export function preparePieData(args) {
             });
             return acc;
         }, []);
-        data.segments = pieGenerator(segments);
+        const explodeDirArc = arc().innerRadius(0).outerRadius(1).cornerRadius(0);
+        data.segments = pieGenerator(segments).map((seg) => {
+            var _a, _b;
+            const rawPoint = (_b = (_a = seg.data.series) === null || _a === void 0 ? void 0 : _a.data) !== null && _b !== void 0 ? _b : {};
+            const explodeOffset = typeof rawPoint.offset === 'number' ? rawPoint.offset : 0;
+            if (!explodeOffset) {
+                return seg;
+            }
+            const [vx, vy] = explodeDirArc.centroid(seg);
+            const len = Math.sqrt(vx * vx + vy * vy) || 1;
+            return Object.assign(Object.assign({}, seg), {
+                explodeDx: (vx / len) * explodeOffset,
+                explodeDy: (vy / len) * explodeOffset,
+            });
+        });
         return data;
     };
     const getLabels = async ({ series }) => {
diff --git a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/utils.js b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/utils.js
index a689537..f9b9254 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/utils.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/hooks/useShapes/utils.js
@@ -1,6 +1,6 @@
 import { path, select } from 'd3';
 import get from 'lodash/get';
-import { getDataCategoryValue } from '../../utils';
+import { getDataCategoryValue, isBandScale } from '../../utils';
 const ONE_POINT_DOMAIN_DATA_CAPACITY = 3;
 export function getXValue(args) {
     const { point, points, xAxis, xScale } = args;
@@ -8,7 +8,23 @@ export function getXValue(args) {
         const xBandScale = xScale;
         const categories = get(xAxis, 'categories', []);
         const dataCategory = getDataCategoryValue({ axisDirection: 'x', categories, data: point });
-        return (xBandScale(dataCategory) || 0) + xBandScale.step() / 2;
+        const base = xBandScale(dataCategory);
+        if (base === undefined || base === null) {
+            return null;
+        }
+        if (!isBandScale(xBandScale)) {
+            return base;
+        }
+        const bw = xBandScale.bandwidth();
+        const domain = xBandScale.domain();
+        const catStr = String(dataCategory);
+        if (domain.length && String(domain[0]) === catStr) {
+            return base;
+        }
+        if (domain.length && String(domain[domain.length - 1]) === catStr) {
+            return base + bw;
+        }
+        return base + bw / 2;
     }
     let xLinearScale = xScale;
     const [xMinDomain, xMaxDomain] = xLinearScale.domain();
diff --git a/node_modules/@gravity-ui/charts/dist/esm/types/chart/legend.d.ts b/node_modules/@gravity-ui/charts/dist/esm/types/chart/legend.d.ts
index 6b98a41..1612b3c 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/types/chart/legend.d.ts
+++ b/node_modules/@gravity-ui/charts/dist/esm/types/chart/legend.d.ts
@@ -9,6 +9,14 @@ export interface ChartLegend {
      * @default 'discrete'
      */
     type?: 'discrete' | 'continuous';
+    /**
+     * The position of the legend relative to the chart area.
+     * - 'bottom' (default): horizontal legend below the chart
+     * - 'right': vertical panel on the right side of the chart
+     *
+     * @default bottom
+     */
+    position?: 'bottom' | 'right';
     /**
      * The horizontal alignment of the legend box within the chart area.
      *
diff --git a/node_modules/@gravity-ui/charts/dist/esm/utils/chart/axis-generators/bottom.js b/node_modules/@gravity-ui/charts/dist/esm/utils/chart/axis-generators/bottom.js
index 952800a..1c56092 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/utils/chart/axis-generators/bottom.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/utils/chart/axis-generators/bottom.js
@@ -1,8 +1,23 @@
 import { path, select } from 'd3';
-import { getAxisItems, getXAxisOffset, getXTickPosition } from '../axis';
+import { getAxisItems, getXAxisOffset, getXTickPosition, isBandScale } from '../axis';
 import { calculateCos, calculateSin } from '../math';
 import { getLabelsSize, setEllipsisForOverflowText } from '../text';
 const AXIS_BOTTOM_HTML_LABELS_DATA_ATTR = 'data-axis-bottom-html-labels';
+function isAxisBottomTickZero(d) {
+    if (d === 0 || d === '0') {
+        return true;
+    }
+    if (typeof d === 'number' && Number.isFinite(d) && !Number.isNaN(d) && Math.abs(d) < 1e-9) {
+        return true;
+    }
+    if (typeof d === 'string') {
+        const n = Number(d);
+        if (Number.isFinite(n) && Math.abs(n) < 1e-9) {
+            return true;
+        }
+    }
+    return false;
+}
 function addDomain(selection, options) {
     const { size, color } = options;
     const domainPath = selection
@@ -11,7 +26,7 @@ function addDomain(selection, options) {
         .enter()
         .insert('path', '.tick')
         .attr('class', 'domain')
-        .attr('d', `M0,0V0H${size}`);
+        .attr('d', `M0,0H${Math.round(size)}`);
     if (color) {
         domainPath.style('stroke', color);
     }
@@ -23,6 +38,7 @@ function appendSvgLabels(args) {
         .append('text')
         .html(ticks.labelFormat)
         .style('font-size', ((_a = ticks.labelsStyle) === null || _a === void 0 ? void 0 : _a.fontSize) || '')
+        .style('font-weight', (_a === null || _a === void 0 ? void 0 : _a.fontWeight) || '400')
         .attr('fill', 'currentColor')
         .attr('text-anchor', () => {
         if (ticks.rotation) {
@@ -45,7 +61,7 @@ function appendSvgLabels(args) {
         let elementX = 0;
         // add an ellipsis to the labels that go beyond the boundaries of the chart
         // and remove overlapping labels
-        labels
+        const sortedLabelNodes = labels
             .nodes()
             .map((element) => {
             const r = element.getBoundingClientRect();
@@ -57,9 +73,10 @@ function appendSvgLabels(args) {
         }, {})
             .sort((a, b) => {
             return a.left - b.left;
-        })
-            .forEach(function (item, i, nodes) {
-            var _a, _b, _c, _d;
+        });
+        const skipOverlapCulling = !ticks.rotation && sortedLabelNodes.length <= 120;
+        sortedLabelNodes.forEach(function (item, i, nodes) {
+            var _a, _b, _c;
             const { node, left, right: currentElementPositionRigth } = item;
             const currentElement = node;
             if (i === 0) {
@@ -78,23 +95,31 @@ function appendSvgLabels(args) {
             }
             else {
                 if (left < elementX) {
-                    (_c = currentElement.closest('.tick')) === null || _c === void 0 ? void 0 : _c.remove();
-                    return;
-                }
-                elementX = currentElementPositionRigth + ticks.labelsPaddings;
-                if (i === nodes.length - 1) {
-                    const prevElement = (_d = nodes[i - 1]) === null || _d === void 0 ? void 0 : _d.node;
-                    const text = select(currentElement);
-                    const prevElementPosition = prevElement === null || prevElement === void 0 ? void 0 : prevElement.getBoundingClientRect();
-                    const lackingSpace = Math.max(0, currentElementPositionRigth - right);
-                    if (lackingSpace) {
-                        const remainSpace = right - ((prevElementPosition === null || prevElementPosition === void 0 ? void 0 : prevElementPosition.right) || 0) - ticks.labelsPaddings;
-                        const translateX = -lackingSpace;
-                        text.style('transform', `translate(${translateX}px,${translateY}px)`);
-                        setEllipsisForOverflowText(text, remainSpace);
+                    if (!skipOverlapCulling) {
+                        (_c = currentElement.closest('.tick')) === null || _c === void 0 ? void 0 : _c.remove();
+                        return;
                     }
                 }
+                elementX = currentElementPositionRigth + ticks.labelsPaddings;
+            }
+        });
+    }
+    const zvn = ticks.zeroLabelVerticalNudgePx;
+    const zhn = ticks.zeroLabelHNudgePx;
+    const extraDy = typeof zvn === 'number' && Number.isFinite(zvn) ? zvn : 0;
+    const manualDx = typeof zhn === 'number' && Number.isFinite(zhn) ? zhn : 0;
+    const extraDx = manualDx;
+    if (extraDy !== 0 || extraDx !== 0) {
+        ticksSelection.each(function (d) {
+            if (!isAxisBottomTickZero(d)) {
+                return;
+            }
+            const text = select(this).select('text');
+            if (!text.node()) {
+                return;
             }
+            // <text> already has style('transform', transform); dy/dx are easy to lose vs CSS transform — append translate(px).
+            text.style('transform', `${transform} translate(${extraDx}px, ${extraDy}px)`);
         });
     }
 }
@@ -147,7 +172,7 @@ function appendHtmlLabels(args) {
     });
 }
 export async function axisBottom(args) {
-    const { boundsOffsetLeft = 0, boundsOffsetTop = 0, domain, htmlLayout, leftmostLimit = 0, scale, ticks: { count: ticksCount, items: tickItems, labelFormat = (value) => String(value), labelsHeight = 0, labelsHtml, labelsLineHeight, labelsMargin = 0, labelsMaxWidth = Infinity, labelsPaddings = 0, labelsStyle, maxTickCount, rotation = 0, tickColor, }, } = args;
+    const { boundsOffsetLeft = 0, boundsOffsetTop = 0, domain, htmlLayout, leftmostLimit = 0, scale, ticks: { count: ticksCount, items: tickItems, labelFormat = (value) => String(value), labelsHeight = 0, labelsHtml, labelsLineHeight, labelsMargin = 0, labelsMaxWidth = Infinity, labelsPaddings = 0, labelsStyle, maxTickCount, rotation = 0, tickColor, zeroLabelVerticalNudgePx: ticksZeroLabelVNudgePx, zeroLabelHNudgePx: ticksZeroLabelHNudgePx, }, } = args;
     const htmlSelection = select(htmlLayout);
     const offset = getXAxisOffset();
     const position = getXTickPosition({ scale, offset });
@@ -176,15 +201,39 @@ export async function axisBottom(args) {
         }
         const tickPath = path();
         tickItems === null || tickItems === void 0 ? void 0 : tickItems.forEach(([start, end]) => {
-            tickPath.moveTo(0, start);
-            tickPath.lineTo(0, end);
+            const nearAxis = Math.max(start, end);
+            const farAxis = Math.min(start, end);
+            const liftedStart = nearAxis === 0 ? -2 : nearAxis;
+            tickPath.moveTo(0, liftedStart);
+            tickPath.lineTo(0, farAxis);
         });
+        const domainBand = isBandScale(scale) ? scale.domain() : [];
+        const firstBandDomainVal = domainBand.length ? domainBand[0] : null;
+        const lastBandDomainVal = domainBand.length ? domainBand[domainBand.length - 1] : null;
+        const bandWidth = isBandScale(scale) ? scale.bandwidth() : 0;
+        const bandRange = isBandScale(scale) ? scale.range().map(Number) : [];
+        const xPlot0 = bandRange.length ? Math.min(...bandRange) : 0;
+        const xPlot1 = bandRange.length ? Math.max(...bandRange) : domain.size;
+        const singleBandDomain = Boolean(firstBandDomainVal != null &&
+            lastBandDomainVal != null &&
+            bandWidth > 0 &&
+            String(firstBandDomainVal) === String(lastBandDomainVal));
+        const xBandMid = Math.round((xPlot0 + xPlot1) / 2);
         const htmlLabelsData = labelsHtml
             ? values.map((v) => {
                 var _a;
+                const atBandRightEdge = !singleBandDomain &&
+                    lastBandDomainVal != null &&
+                    bandWidth > 0 &&
+                    String(v) === String(lastBandDomainVal);
+                const tickLeft = singleBandDomain
+                    ? xBandMid
+                    : atBandRightEdge
+                        ? xPlot1
+                        : position(v) + offset;
                 return {
                     content: String(v),
-                    left: position(v) + offset + boundsOffsetLeft,
+                    left: tickLeft + boundsOffsetLeft,
                     top: Math.abs(((_a = tickItems === null || tickItems === void 0 ? void 0 : tickItems[0]) === null || _a === void 0 ? void 0 : _a[1]) || 0) + labelsMargin + boundsOffsetTop,
                 };
             })
@@ -196,13 +245,28 @@ export async function axisBottom(args) {
             .join('g')
             .attr('class', 'tick')
             .attr('transform', function (d) {
-            const left = position(d) + offset;
-            return `translate(${left}, ${top})`;
+            const left = Math.round(position(d) + offset);
+            return `translate(${left}, ${Math.round(top)})`;
         });
         ticks
             .append('path')
             .attr('d', tickPath.toString())
-            .attr('stroke', tickColor !== null && tickColor !== void 0 ? tickColor : 'currentColor');
+            .attr('stroke', tickColor !== null && tickColor !== void 0 ? tickColor : 'currentColor')
+            .style('shape-rendering', 'crispEdges')
+            .style('stroke-width', '1');
+        if (singleBandDomain && firstBandDomainVal != null && bandWidth > 0) {
+            ticks
+                .filter((d) => String(d) === String(firstBandDomainVal))
+                .attr('transform', `translate(${xBandMid}, ${Math.round(top)})`);
+        }
+        if (lastBandDomainVal != null &&
+            bandWidth > 0 &&
+            String(lastBandDomainVal) !== String(firstBandDomainVal)) {
+            const lastLeft = Math.round(xPlot1);
+            ticks
+                .filter((d) => String(d) === String(lastBandDomainVal))
+                .attr('transform', `translate(${lastLeft}, ${Math.round(top)})`);
+        }
         // Remove tick that has the same x coordinate like domain
         selection
             .selectAll('.tick')
@@ -239,6 +303,8 @@ export async function axisBottom(args) {
                     labelsPaddings,
                     labelsStyle,
                     rotation,
+                    zeroLabelVerticalNudgePx: ticksZeroLabelVNudgePx,
+                    zeroLabelHNudgePx: ticksZeroLabelHNudgePx,
                 },
                 transform,
                 translateY,
diff --git a/node_modules/@gravity-ui/charts/dist/esm/utils/chart/axis.js b/node_modules/@gravity-ui/charts/dist/esm/utils/chart/axis.js
index 46ec3e5..ec8fa45 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/utils/chart/axis.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/utils/chart/axis.js
@@ -11,17 +11,22 @@ export function getTicksCount({ axis, range }) {
 export function isBandScale(scale) {
     return 'bandwidth' in scale && typeof scale.bandwidth === 'function';
 }
+/** D3 scalePoint (e.g. category X for line charts): uniform spacing, domain endpoints at range ends. */
+export function isPointScale(scale) {
+    return Boolean(scale && typeof scale.step === 'function' && !isBandScale(scale));
+}
 export function getScaleTicks(scale, ticksCount) {
     if ('ticks' in scale && typeof scale.ticks === 'function') {
         return scale.ticks(ticksCount);
     }
-    if (isBandScale(scale)) {
+    if (isBandScale(scale) || isPointScale(scale)) {
         return scale.domain();
     }
     return [];
 }
 export function getXAxisOffset() {
-    return typeof window !== 'undefined' && window.devicePixelRatio > 1 ? 0 : 0.5;
+    // Fixed offset for consistent X geometry across monitor/laptop.
+    return 0;
 }
 function number(scale) {
     return (d) => Number(scale(d));
@@ -36,8 +41,79 @@ function center(scale, offset) {
 export function getXTickPosition({ scale, offset }) {
     return isBandScale(scale) ? center(scale, offset) : number(scale);
 }
+function isDenseConsecutiveIntegerDomain(domain) {
+    if (!domain || domain.length < 2) {
+        return false;
+    }
+    const nums = domain.map((d) => Number(d));
+    if (!nums.every((n) => Number.isFinite(n) && n === Math.trunc(n))) {
+        return false;
+    }
+    const min = Math.min(...nums);
+    const max = Math.max(...nums);
+    if (max - min + 1 !== nums.length) {
+        return false;
+    }
+    return new Set(nums).size === nums.length;
+}
+function niceStepInt(rough) {
+    if (!Number.isFinite(rough) || rough <= 0) {
+        return 1;
+    }
+    const exp = Math.floor(Math.log10(rough));
+    const pow = Math.pow(10, exp);
+    const frac = rough / pow;
+    let nf = 1;
+    if (frac <= 1)
+        nf = 1;
+    else if (frac <= 2)
+        nf = 2;
+    else if (frac <= 5)
+        nf = 5;
+    else
+        nf = 10;
+    return Math.max(1, Math.round(nf * pow));
+}
 export function getAxisItems({ scale, count, maxCount, }) {
     let values = getScaleTicks(scale, count);
+    if (isBandScale(scale) || isPointScale(scale)) {
+        const range = scale.range();
+        const span = Math.abs(Number(range[1]) - Number(range[0]));
+        const domain = scale.domain();
+        if (isDenseConsecutiveIntegerDomain(domain)) {
+            const nums = domain.map(Number);
+            const minV = Math.min(...nums);
+            const maxV = Math.max(...nums);
+            const spanV = maxV - minV;
+            const minPxPerTick = 32;
+            const maxLabelsByWidth = Math.max(4, Math.floor(span / minPxPerTick));
+            const hardCap = 50;
+            const bandLabelLimit = Math.min(maxLabelsByWidth, hardCap);
+            const targetTicks = Math.max(2, Math.min(bandLabelLimit, domain.length));
+            const rawStep = spanV / Math.max(1, targetTicks - 1);
+            const stepV = niceStepInt(rawStep);
+            const tickStrs = new Set();
+            tickStrs.add(String(maxV));
+            tickStrs.add(String(minV));
+            const start = Math.ceil(minV / stepV) * stepV;
+            for (let v = start; v <= maxV; v += stepV) {
+                tickStrs.add(String(v));
+            }
+            const domainIndex = new Map(domain.map((d, i) => [String(d), i]));
+            return Array.from(tickStrs)
+                .filter((s) => domainIndex.has(s))
+                .sort((a, b) => (domainIndex.get(a) ?? 0) - (domainIndex.get(b) ?? 0));
+        }
+        const minPxPerTick = 32;
+        const maxLabelsByWidth = Math.max(4, Math.floor(span / minPxPerTick));
+        const hardCap = 50;
+        const bandLabelLimit = Math.min(maxLabelsByWidth, hardCap);
+        if (values.length > bandLabelLimit) {
+            const step = Math.ceil(values.length / bandLabelLimit);
+            values = values.filter((_, i) => i % step === 0 || i === values.length - 1);
+        }
+        return values;
+    }
     if (maxCount && values.length > maxCount) {
         const step = Math.ceil(values.length / maxCount);
         values = values.filter((_, i) => i % step === 0);
@@ -137,3 +213,4 @@ export function getLabelFormatter({ axis, scale }) {
         });
     };
 }
+
diff --git a/node_modules/@gravity-ui/charts/dist/esm/utils/chart/index.js b/node_modules/@gravity-ui/charts/dist/esm/utils/chart/index.js
index b9c7bf0..997ab0e 100644
--- a/node_modules/@gravity-ui/charts/dist/esm/utils/chart/index.js
+++ b/node_modules/@gravity-ui/charts/dist/esm/utils/chart/index.js
@@ -206,13 +206,10 @@ const extractCategoryValue = (args) => {
     if (typeof dataCategory === 'number') {
         categoryValue = categories[dataCategory];
     }
-    if (isNil(categoryValue)) {
-        throw new Error('It seems you are trying to get non-existing category value');
-    }
-    return categoryValue;
+    // Return null instead of throwing — caller must guard against null
+    return isNil(categoryValue) ? null : categoryValue;
 };
 export const getDataCategoryValue = (args) => {
     const { axisDirection, categories, data } = args;
-    const categoryValue = extractCategoryValue({ axisDirection, categories, data });
-    return categoryValue;
+    return extractCategoryValue({ axisDirection, categories, data });
 };
